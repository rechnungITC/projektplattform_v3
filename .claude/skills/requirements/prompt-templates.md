> Originally from V2 (`.claude/skills/prompt-templates.md`). Stack-specific references retained; treat as principle, not literal.

# Prompt Templates

## Five-part prompt structure
1. Identity
2. Task
3. Context
4. Constraints
5. Output format

## Rule
Persistent context lives in repository files.
Per-task direction lives in the prompt.
