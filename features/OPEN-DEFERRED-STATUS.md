# Open, Deferred, and Partial Feature Status

**Last Updated:** 2026-08-20

This file is the cross-feature status register referenced by `features/INDEX.md`. It keeps follow-ups, MVP partials, and bookkeeping conflicts visible without overloading individual feature rows.

## Planned Followups

| PROJ | Status | Current reconciliation |
|---|---|---|
| PROJ-71 | Planned (Followup) | OCR for scan PDFs from PROJ-70-γ Y-1. Still pilot-feedback-driven; PROJ-80 RAG should only surface extraction failures in V1. |
| PROJ-72 | Planned (Followup) | Streaming/chunked parse or worker queue for upload scaling. Still real-load-driven; not a blocker for current 25 MB parser flow. |
| PROJ-73 | Planned (Followup, reduced) | More parser formats. MD/TXT and EML/MSG are now covered by PROJ-70 γ/δ for kickoff ingestion; remaining practical gap is PPTX/XLSX and later DMS/RAG formats. |
| PROJ-74 | Approved | Supply-chain audit CI slice selected as the independent item: workflow + `npm run audit:prod` + Sentry parser-output scrubber implemented and locally verified; remaining handoff is `SNYK_TOKEN` + branch-protection required-check setup after merge. **Nachtrag 2026-08-13 (PROJ-147):** der hier genannte Handoff ist zweigeteilt und die eine Haelfte ist tot. `SNYK_TOKEN` wird **bewusst nicht** gesetzt (Snyk zurueckgestellt, kein externer Account gewollt) — der Required-Check wurde aber trotzdem eingetragen und meldet seither gruen, ohne zu scannen. Damit war PROJ-74s AC 4 ("instead of silently pretending Snyk ran") durch das Enrollment gebrochen. Der in PROJ-74 vorgesehene account-freie Ersatz ist jetzt als **PROJ-147** gebaut (Dependency Review); das dort verlorene Folgeticket (auf PROJ-94/PROJ-67 gebucht, beide sachfremd) ist damit eingeloest. |
| PROJ-75 | **Geschlossen 2026-08-20 — Deployed / Scope `full`** (war `mvp`: Ingestion geschuetzt, Backfill-**Ausfuehrung** offen; die Ausfuehrung ist nachgeholt, siehe PROJ-Y-75a-Zeile) | **Korrigiert 2026-08-13.** Hier stand „Planned (Followup) — still open", was als Lifecycle-Aussage falsch war (Code ist seit 2026-07-21 als `v2.15.0-PROJ-75` deployed), in der **Sache** aber richtiger als das `Deployed`-Label im INDEX: der Ingestion-Pfad ist fail-closed ausgeliefert und schuetzt jeden neuen Upload, der Block „Backfill (Bestands-Rows)" (AC-75.6–75.10) ist jedoch **nie gegen die realen Zeilen gelaufen**. Deshalb Scope `mvp` statt `full`, Omission registriert als **PROJ-Y-75a**. Gemessen am 2026-08-13: 15 Context-Sources, **0** je volltext-geprueft (`full_text_classified_at is null` fuer alle), davon **13 im Kundenmandanten** `IT-Couch GmbH` mit `privacy_class = 2` und Excerpt-Laenge **exakt 8000** (also alle abgeschnitten); die restlichen 2 sind E2E-Zeilen mit 42 Zeichen. |
| PROJ-Y-75a | **Geschlossen 2026-08-20 — ausgefuehrt, PROJ-75 auf `full`** | **Ergebnis: 15 geprueft / 0 hochgestuft / 0 unverifiziert / 15 gescreent / 0 verbleibend** (HTTP 200, 20,0 s), Nachzustand in der DB gegengeprueft. Idempotenz live bewiesen (Lauf 2: 0 geprueft, 1,8 s — der Laufzeitsprung belegt nebenbei, dass Lauf 1 echte Downloads/Parses war), Negativkontrolle ohne Bearer 401. **Kein Zeitstempel-Kurzschluss:** der Route-Zweig, der ohne Re-Parse stempelt, greift nur bei fehlender Datei — alle 15 Zeilen hatten eine, also lief jede ueber den Volltext. **Ergebnis bewusst qualifiziert:** die 13 Kundenzeilen sind **dasselbe** DOCX (`raw_length` 12 044 bei allen), neu geprueft wurden 4 044 Zeichen echten Neutexts pro Zeile — „0 hochgestuft" ist eine Aussage ueber den Bestand, nicht ueber den Klassifizierer (dessen Greifen im Rumpf ist separat durch einen Test mit **echtem** Klassifizierer belegt). AC-75.7/75.8 hatten keinen Anlass, sind also erfuellt ohne gegen echte Daten ausgeuebt worden zu sein. **Rest: D-3** (Ende-zu-Ende-Upload in Prod) — Nachweistiefe, kein Kriterium, deshalb kein neuer Followup. *Ursprünglicher Auftrag zur Nachvollziehbarkeit:* Backfill einmal gegen die 13 Bestands-Zeilen des Kundenmandanten ausfuehren und die AC-75.10-Kennzahl (upgraded / unverified / remaining) festhalten. Ein Aufruf: `curl -X POST https://projektplattform-v3.vercel.app/api/context-sources/reclassify-backfill -H "Authorization: Bearer $CRON_SECRET" -H "content-type: application/json" -d '{"limit":500}'` Danach `full_text_classified_at` fuer alle 15 Zeilen gesetzt; hochgestufte Zeilen tragen `privacy_class = 3`, nicht sicher screenbare tragen `classification_unverified = true` (AC-75.8). **Nicht aus dieser Session ausfuehrbar:** `CRON_SECRET` liegt nicht in `.env.local` (geprueft), die Route antwortet ohne Bearer korrekt mit 401 — bewusst beim Repo-Eigner. Schliesst die Omission und hebt PROJ-75 auf `full` (neuer QA-/Deploy-Pass laut Regel). |
| PROJ-45-β | **Deployed 2026-08-19 / Scope `alpha`** — Tag `v2.61.0-PROJ-45-beta`, PR #402 (squash) → main `740b16b`. Kein Runtime-DB-Change beim Merge (Migration seit `/backend` in Prod); Vercel-Produktions-Deployment `dpl_C9Mk4gxt68CRPzZ9AEUtioe96iU9` **READY**, gebaut aus genau diesem SHA, 0 Laufzeitfehler im 24-h-Fenster; Post-Deploy-Smoke über alle **sieben** neuen Flächen exakt **307** mit Rumpf `Redirecting...` und ohne Leck, Routen-Existenz über das **Build-Log des ausgelieferten Deployments** belegt (ein 307 allein beweist sie nicht — der Proxy greift auch auf erfundene Pfade). Prod eigenständig nachgemessen: 2 Tabellen mit RLS, **0 Schreib-Policies**, 8 Funktionen, **5** Trigger, `anon` **und PUBLIC** ohne EXECUTE über alle 19 geprüften Funktionen, Register 94/76/63 mit 14 erhaltenen Geschwister-Zweigen, Advisors 0 ERROR, 0 Rückstände. **Ein Dokumentationsfehler korrigiert** (D-45β-DEPLOY-1): die `/backend`-Notiz sagte „sechs Trigger“, die Migration erzeugt **fünf** — Zahl war nie geprüft, kein Produktfehler. Vorher: `/qa` PASS 2026-08-19, 0 Critical / 0 High (21/22 AC, alle 12 Härtungskriterien; **AC-45β.18 offen und nach δ verschoben**, siehe δ-Zeile). Der Zwei-Personen-Durchlauf ist in drei getrennten authentifizierten Sitzungen **gefahren**, die fünf Tech-Design-Risiken einzeln geprüft, ein **echter PDF-Druck** liegt vor; Live-Pentest **53/53 PASS / 0 FAIL** nach Härtung eines Vektors, der bis dahin nur einen Wert ausgab statt zu prüfen (F-1), Regressionen wörtlich α 16/16 · PROJ-Y-45a 9/9 · PROJ-103 A–G 7/7, Visual-Baselines 9/9 ohne Neuaufnahme, Advisors 0 ERROR, 0 Rückstände über zehn Zähler. Zwei offene Low/Info-Befunde als **PROJ-Y-45d** und **PROJ-Y-45e** registriert. Der Scope ist in `/deploy` als **`alpha`** vergeben: `full` scheitert an der zurückgestellten Original-Anforderung **AC-45β.18**, und die Ausnahme „Waived criterion“ greift nicht — sie verlangt „nothing was deferred“, hier ist ausdrücklich etwas zurückgestellt (Begründung an allen vier Bedingungen in der Spec). Vorher: `/backend` + `/frontend` live 2026-08-18 (Migration `20260818104358` in Prod), Architected 2026-08-17, Requirements 2026-08-14) | **Mängelmanagement.** 22 Akzeptanzkriterien, 8 Nutzer-Locks (L9–L16), 9 Edge Cases; die 4 offenen Architekturfragen sind beantwortet. **Zwei Befunde haben den Zuschnitt geändert.** (1) **L12 war nicht baubar:** die Engpass-Sicht aus PROJ-103 trägt `requiresProjectType: "ma"` und `filterSectionsByProjectType` vergleicht strikt auf **einem** Projekttyp — in einem Bauprojekt existiert die Fläche gar nicht; zudem weist die Out-of-Scope-Tabelle der Spec die PROJ-103-Anbindung ohnehin δ zu. Nutzer-Entscheid: **AC-45β.18 wandert nach PROJ-45-δ** (Zeile unten), β liefert Überfälligkeit in der Mängelliste (AC-45β.17/.19) plus Zähler je Gewerk. β kann damit nicht `full` werden — als Sub-Slice ohnehin `alpha`. (2) **Die Rechte weichen in beide Richtungen ab:** „Projektleitung oder Mandanten-Administration" schließt den Projekt-`editor` **aus**, den das Hausrecht `edit` einschließt — Nutzer-Entscheid: so gewollt, gewährleistungsrelevante Fristen in einer Hand. **Q-β1 entschärft:** die Vorlage `dd_findings` trägt live ausschließlich Lese-Regeln, geschrieben wird nur über Funktionen — „Betrachter darf anlegen" ist damit eine Rollenprüfung *innerhalb* der Anlege-Funktion und keine gelockerte Zugriffsregel; nichts Bestehendes wird aufgeweicht. **Q-β4 kollidiert real:** beide projektbezogenen Entfernen-Pfade aus α bilden heute jeden Fehler außer `42501` auf 500 ab, ein `RESTRICT` für Mängel liefe dort in einen rohen Datenbanktext statt in die von AC-45β.21 verlangte benennende Meldung — behoben durch das 409-Muster, das α eine Ebene höher schon fährt; die benennende Abfrage muss den **Teilbaum** absuchen, weil `parent_id` kaskadiert. 12 blockierende Härtungskriterien (AC-45βH-1…12), kein neues Paket, Reihenfolge `/backend` → `/frontend` → `/qa`. **Befund beim Erden:** L3s ursprüngliche Begründung ist zur Hälfte überholt — α hat drei der vier genannten Lücken selbst geschlossen (`section_id`, `trade_id`, `due_date`), echt übrig bleiben nur Nachunternehmer und Prüf-Stufe. L3 bleibt trotzdem, aber **fachlich** begründet: ein Mangel ist keine geplante Arbeit und würde im Backlog Velocity, Burndown und WBS-Rollups verfälschen. Kern der Slice ist das **Zwei-Akteur-Gate** (wer „erledigt" meldet, darf nicht selbst „geprüft" setzen; Rückweisung wirft zurück statt ein Duplikat zu erzeugen). Zwei bewusste Abweichungen, beide architekturrelevant: **L15** lässt auch Betrachter erfassen (weicht vom Hausmuster ab, CIA-pflichtig) und **L16** sperrt das Entfernen statt `SET NULL` wie in α (ein Mangel ohne Adressaten ist gewährleistungsrechtlich wertlos). |
| PROJ-45-γ | **Deployed 2026-08-20 / Feature-Scope bleibt `alpha`** — Tag `v2.70.0-PROJ-45-gamma`, PR #422 (squash) → main `31aef7f`; damit ist die zurückgestellte Original-Anforderung **ST-02 erledigt** | **Abnahmen** — Abnahmeprotokoll mit Status, Termin, verantwortlicher Rolle und Belegverweis; eine fehlgeschlagene Abnahme erzeugt prüfbare Vorschläge für Mangel oder Risiko. **Zurückgestellte Original-Anforderung** (ST-02), unverändert offen — die Slice ist jetzt **spezifiziert, nicht gebaut**: 29 Akzeptanzkriterien, 7 Nutzer-Locks (L17–L23), 10 Edge Cases, 6 offene Architekturfragen, im γ-Block der [PROJ-45-Spec](PROJ-45-construction-extension.md). **Die benannte Vorlage trägt nur zur Hälfte, live gemessen:** `deliverable_approvals` (PROJ-105) hat in Prod **0 Zeilen** und arbeitet sequenziell mit **einem** Freigeber je Stufe und binärem Ergebnis — eine Abnahme ist **ein gemeinsamer Termin** mit dreiwertigem Ergebnis (abgenommen · unter Vorbehalt · verweigert) und setzt eine Rechtsfrist in Gang. Übernommen wird die unveränderliche Ereignis-Tabelle, das Schreiben nur über Funktionen und „höchstens ein offener Vorgang je Bezug"; **nicht** die Stufen-Mechanik. `milestones` scheidet ebenfalls aus (kein Ergebnis, keine Teilnehmer, keine Vorbehalte, keine Frist). **Sieben Nutzer-Entscheide:** genau **ein** Bezug je Abnahme (Gewerk **oder** Abschnitt, Gesamtabnahme = Wurzel-Abschnitt) · zweistufig angesetzt → Ergebnis · ein Ergebnis ist endgültig, die Nachabnahme ist ein **neuer** Datensatz mit Verweis (Invariante #5) · Vorbehalte koppeln an die β-Mängel (bestehende anhaken **und** neue über die bestehende β-Anlegefunktion, keine zweite Mängelliste) · Gewährleistung wird **gerechnet und gezeigt, nicht überwacht** · Protokollieren nur Projektleitung/Bauleitung oder Mandanten-Administration — **verschärfend gegenüber L15**, kein Vier-Augen-Tor · Ausgabe als Druckseite plus **ein** Beleg je Abnahme (externer Link **oder** DMS-Knoten) und strukturierte Teilnehmer. **Gemessener Preis des externen Belegwegs:** `external_document_links` trägt live **6** Objektarten und bekäme eine siebte, `external_link_parent_ctx` einen Bau-Zweig — der Auflöser gibt je Zweig ein `confidentiality_level` zurück, das Bauprojekte per α-Entscheid nicht haben. **Entschieden im Tech Design (Q-γ1): eigene Bau-Beleg-Ablage statt Erweiterung**, mit derselben Begründung wie α bei den Gewerken; wiederverwendet wird die Adressprüfung als reine Funktion. Damit entfällt der einzige CIA-Anlass (Q-γ6). **Drei Akzeptanzkriterien wurden dabei korrigiert** — AC-45γ.1 (Gesamtabnahme über den Wurzel-Abschnitt war ohne Abschnittsbaum nicht baubar → Anker wird optional), AC-45γ.9 (der Beleg hätte mit eingefroren, obwohl das unterschriebene Protokoll erst danach zurückkommt) und AC-45γ.25 (Teilnehmer-Achse „Projektmitglied" war doppelt). **Vierter Befund, Bestandsarbeit:** die Entfernen-Meldung aus α/β nennt wörtlich „Mängel" und den Code `defects_present` — mit γ würde sie **falsch** statt bloss unvollständig; γ verallgemeinert sie mit (neuer Code `references_present`, zwei INVOKER-Auskünfte, die Art und Bezeichnung nennen), der breitere Fall bleibt PROJ-Y-45b. **`/backend` live 2026-08-19:** 2 Migrationen in Prod, 4 Tabellen mit **0 Schreib-Policies**, 9 Funktionen, Objektarten 94→95, 6 Routen, 43 neue Tests. **Vier Befunde**, davon zwei, die ohne Nachmessen still geblieben wären: die eigene PUBLIC-ACL-Post-Condition war falsch und brach den ersten Anwendungsversuch ab (Migration lief atomar zurück); die α-Regression war seit **einem Tag** scope-blind, weil die β-QA-Fixture erstmals echte Bau-Zeilen in Prod angelegt hat (gehärtet statt abgeschwächt, 5/5); das Teilnehmer-Modell hätte **das Löschen eines Stakeholders blockiert** (vor dem Anwenden gefunden, per Namensschnappschuss gelöst); und — das gewichtigste — eine **parallele Session** hat am selben Tag β's Ereignis-Wächter gehärtet (PROJ-Y-148d), γ hatte die schwache Form geerbt und hätte die Lücke eine Tabelle weiter neu aufgerissen. Fix-forward in Prod; `construction_acceptance_events` ist damit die **sechste** unveränderliche Insel aus PROJ-Y-148a, mit **gemessenem** `blocksHardDelete: true` in dessen Registry eingetragen und die drei eingefrorenen Listen nachgezogen. Neue Folgearbeit: **PROJ-Y-45f** (β's `construction_section_blocking_defects` ist nach dem γ-Deploy ohne Aufrufer), **PROJ-Y-45g** (Beleg als Dokumentknoten auswählbar machen — Datenbankseite steht und ist geprüft, es fehlt allein der Picker), **PROJ-Y-45h** (Teardown für die Bau-E2E-Lane: γs Abnahmen **und** β's Mängel sind nicht mehr löschbar, beide Specs brauchen einen sanktionierten, mandantengebundenen Aufräumschritt) und **PROJ-Y-45i** (verdeckter Titel im Ladezustand der Abnahme-Detailansicht, WCAG). **`/qa` PASS 2026-08-20 (0 Critical / 0 High / 0 Medium → Approved):** 29/29 AC, 12/12 Härtungskriterien; der authentifizierte Durchlauf ist gefahren (3× 3/3, eigene Bau-Fixture-Lane) und ein **echter PDF-Druck** liegt vor. γ-Pentest 60/60, Rot-Team 11/11 neu, Regressionen α 18/18 · PROJ-Y-45a 9/9 · PROJ-103 7/7 wörtlich, β 52/53, 0 Rückstände über 14 Zähler, Advisors 0 ERROR. **F-γ4 (fremd verursacht; im `/deploy`-Lauf zur Hälfte erledigt):** PROJ-Y-148d hatte β's QA-Teardown **und** β's Pentest-Vektor Z gebrochen — beide gemessen (β-Spec 5 failed / 1 did not run / 12 passed, Stacktrace auf `deleteOrThrow`; Vektor Z `FAIL(42501)`). **Vektor Z ist erledigt:** 148d hat ihn in seinem eigenen Merge `b38c11d` umgedreht statt stehengelassen, auf dem Deploy-Stand gegen Prod `Z_project_hard_delete_blocked=PASS(42501)` nachgemessen → **β ist wieder 53/53**, die QA-Zahl 52/53 war zum Messzeitpunkt richtig und ist überholt. **Offen bleibt allein der Teardown** (auf dem Deploy-Stand erneut 5 failed / 1 did not run / 12 passed; 148d hat weder `tests/PROJ-45-beta-defects.spec.ts` noch `tests/fixtures/cleanup.ts` angefasst) — sachlich dieselbe Lücke wie PROJ-Y-45h. Ursache belegt bei 148d, nicht bei γ (0 Definitionen von β's Wächter in γs Migrationen, nur ein Kommentar; β Block 1 wörtlich 32/32 grün). Gehört in den 148d-Zweig. **`/frontend` live 2026-08-20:** Reiter, zwei Masken, Detailansicht, Druckseite, Navigationseintrag, Abnahmestand je Gewerk. **Ein gemessener Befund (F-γ1):** die Gewährleistungsfrist existiert zwangsläufig zweimal (Datenbank schreibt sie fest, die Maske zeigt sie vorher), und die naive TypeScript-Fassung wich am Monatsende ab — Postgres klemmt (`2026-01-31 + 1 Monat = 2026-02-28`), `setUTCMonth` läuft über (2026-03-03); die Maske hätte ein anderes, rechtlich relevantes Fristende angezeigt als gespeichert wird. Behoben über eine geteilte Lib mit Klemmung, fünf live gemessene Datumspaare im Test eingefroren, Rot-Grün ausgeführt. **Deployed 2026-08-20: Tag `v2.70.0-PROJ-45-gamma`, PR #422 (squash) → main `31aef7f`.** Kein Runtime-DB-Change beim Merge (beide Migrationen seit `/backend` in Prod). Vercel-Produktions-Deployment `dpl_3UYP2FbCNTuzuPowxsrZVnB3reaf` **READY**, gebaut aus genau diesem SHA; keine Fehler-Logs über die Lebensdauer des Deployments — Fenster mit ~9 Minuten allerdings kurz, weil eine Parallel-Slice sofort ein neueres Prod-Deployment auslöste (gesagt statt gerundet). Alle sechs Pflicht-Checks grün, darunter der **Schema-Drift-Wächter** (belegt unabhängig, dass die Register-Anker auch in einer frisch aus den Migrationsdateien gebauten Datenbank greifen) und der neue **Funktions-Inventar-Wächter** (Inventar 272 → **283**, gegen Prod gegengezählt). **Post-Deploy-Smoke 13 Flächen exakt 307**, Rumpf `Redirecting...`, kein Leck — und die **Existenz** der Routen ist nicht daraus gefolgert (ein erfundener Pfad antwortet ebenfalls 307, gegengeprüft), sondern am **Build-Log des ausgelieferten Deployments** belegt, in dem alle acht neuen Flächen als Routen stehen und die 17 α/β-Flächen unverändert mitlaufen. **Prod eigenständig nachgemessen:** 4 Tabellen mit RLS, 4 Policies und damit **0 Schreib-Policies**, 11 Funktionen (8 DEFINER, **3 INVOKER**), `anon` **und PUBLIC** ohne EXECUTE über alle 11, 0 Funktionen ohne `search_path`, 5 Trigger, 0 deaktivierte Bau-Trigger, Objektarten **95**, 0 Rückstände, Advisors **0 ERROR**. **Ein eigener Messfehler festgehalten:** die erste Zählung meldete „20 Tabellen“, weil `pg_class` auch Indizes enthält — mit `relkind='r'` sind es 4; nachgezählt, bevor die Zahl irgendwo behauptet wurde. **Scope bleibt `alpha`, nicht `full`:** die Ausnahme „Waived criterion“ scheitert schon an ihrer ersten Bedingung — **AC-45β.18** ist eine zurückgestellte **ursprüngliche** Anforderung mit Ziel-ID (nach δ, D-β1); zusätzlich bleiben δ und ε offen. γs eigene 29 Kriterien sind sämtlich erfüllt, das hebt den Feature-Scope aber nicht. Offen: **PROJ-45-δ/ε**, **PROJ-Y-45b/45c/45d/45e/45f/45g/45h/45i**; nicht γ: β's QA-Teardown im 148d-Zweig. |
| PROJ-45-δ | **Approved — `/qa` PASS 2026-08-21** (`/backend` + `/frontend` live 2026-08-20) (architected + spezifiziert am selben Tag) (Followup, 45); 24 AC + 9 Härtungskriterien, 7 Locks (L24–L30), 6 User Stories, 15 Edge Cases, 6 offene Architekturfragen im δ-Block der [PROJ-45-Spec](PROJ-45-construction-extension.md) | **Bauspezifische Terminsignale** — Abschnittsfortschritt und Gewerk-Blocker. **Zurückgestellte Original-Anforderung** (ST-04) und Träger von **AC-45β.18**, also der Grund, dass PROJ-45 `alpha` statt `full` trägt. **Sieben Messungen gegen den deployten Stand, vier widerlegen eine naheliegende Lesart:** `construction_sections` trägt **kein einziges Datumsfeld** → Abschnittsfortschritt ist abgeleitet, nicht gespeichert; die beiden Ableitungsquellen (`construction_section_phases` M:N und `work_items.section_id`) sind in Prod **beide leer**, ebenso `work_items.trade_id` und `risks.trade_id` — **alle drei additiven α-Verweise sind unbenutzt**, der Leerzustand ist der Normalfall; **kein Projekt trägt Bauachse und Termine** (das einzige lebende Bauprojekt ist die β/γ-QA-Fixture mit `project_method = null`, 0 Arbeitspaketen, 0 Phasen, 0 Meilensteinen, aber 2 Abschnitten); der Gantt (`GanttView`, **1809 Zeilen**) kennt genau **drei** Zeilenarten mit Ziehen/Größe/Abhängigkeiten; und **alle fünf** Auswertungsflächen sind M&A-gegatet, ein Bauprojekt erreicht **keine** davon. **Ein Haus ist offen, aber nicht das erwartete:** `ReportsSection` (PROJ-21) sitzt ungegatet auf der Übersicht mit typisiertem, erweiterbarem `SnapshotContent`. **Nutzer-Entscheid L24: eigene Fläche „Terminsignale" + optionaler Bau-Block im PROJ-21-Status-Report, der Gantt bleibt unberührt** — eine vierte Gantt-Zeilenart wäre teuer **und** würde für jedes heutige Projekt leer rendern. **Die hier zuvor vorgeschriebene Umsetzung von AC-45β.18 ist durch Nutzer-Entscheid L25 überholt:** `requiresProjectType` bleibt **einwertig**, `project_task_bottlenecks` bleibt **unverändert**, die 5 Registry-Tests und der PROJ-103-Pentest (absolute Zahlen) bleiben wörtlich grün — die überfälligen Mängel erscheinen in der δ-Fläche statt in der M&A-Engpass-Sicht. Die **Absicht** des Kriteriums ist damit erfüllt, der **Ort** weicht vom Wortlaut ab; die Abweichung wird dokumentiert, das Kriterium **nicht** umgeschrieben (AC-45δ.16). Die Erhebung zur Schlüsselform bleibt gültig und ist live nachgemessen: Schlüssel `summary · tasks · top_bottlenecks`, jeder `tasks`-Eintrag trägt ein `kind`, das ein Mangel nicht hat → Mängel gehören in einen eigenen Schlüssel. **Ein Befund, der eine naive Umsetzung falsch machen würde:** β und γ führen bewusst **zwei verschiedene** Offen-Begriffe — β's „überfällig" schließt `erledigt` **aus** (`OVERDUE_ELIGIBLE_STATUSES`), γ's „Vorbehalt offen" schließt es **ein** (`ACCEPTANCE_OPEN_DEFECT_STATUSES`, Begründung im Code: „für eine Abnahme ist fertiggemeldet nicht erledigt"). Beide sind doppelt gepinnt (SQL + TS); δ verwendet jeden an seinem Platz und erfindet keinen dritten (L29, AC-45δ.6, AC-45δH-7). **Die hier bei δ geführte Achsen-Entscheidung (CIA-F-4) ist gegenstandslos, nicht offen:** `workstreams` hat **0 Zeilen** und alle fünf Workstream-Auswertungen sind M&A-gegatet — ein Bauprojekt ruft keine davon auf. δ hält das fest statt zu entscheiden; die Frage wird real, sobald eine Auswertung für **beide** Projekttypen freigegeben wird, und gehört dann zu jener Slice. **Weitere Locks:** gerechnetes Gewerk-Signal **neben** der manuellen α-Ampel statt an ihrer Stelle (L26, die Abweichung ist der Ertrag) · Blocker = überfälliger Mangel · verweigerte Abnahme · überfällig angesetzte Abnahme · Abnahme unter Vorbehalt mit offenen Vorbehalten, **„abgesagt" blockiert nicht** (L27, γ hat „absagen" als Korrekturweg gebaut) · Fortschritt aus Arbeitspaketen, sonst aus Phasen, **mit Quellenangabe** (L28 — ohne sie ist „0 %" nicht von „nichts verknüpft" zu unterscheiden, und genau das ist heute der Normalfall) · Mangel ohne Frist wird getrennt gezählt und blockiert nicht (L29) · **keine neue Tabelle, kein Register-Eingriff, kein gespeicherter Signalzustand** (L30). Kein neues Paket. **Architected 2026-08-20 — alle sechs Architekturfragen beantwortet, kein CIA-Pass** (Nutzer-Entscheid, gestützt auf die Messung: kein Paket, keine Tabelle, kein Register-Eingriff; einziger Eingriff in eine deployte Fläche ist der additive Berichts-Block). Geerdet in **vier parallelen Messläufen**. **Q1 → eine gebündelte Auswertungsfunktion** mit vier Schlüsseln; tragend ist nicht Sparsamkeit, sondern **ein Zeitbezug** — alle vier Blöcke rechnen gegen `current_date`, vier getrennte Aufrufe können über Mitternacht auseinanderfallen und die Fläche zeigte Kopfzahlen, die zu ihren Listen nicht passen (Hausmuster ist ohnehin gebündelt: PROJ-131 4 Blöcke, PROJ-132 5, PROJ-103 3). **Q2 → rekursives CTE über `parent_id`** wie β/γ, obwohl α `path` als echtes `ltree` **mit GiST-Index** angelegt hat und `<@` die elegantere Abfrage wäre: dieselbe Frage am **selben** Baum wird in β und γ rekursiv gelöst, `path` ist `nullable` **ohne CHECK**, und Perf ist bei diesen Baumgrössen keine Frage — eine dritte Technik innerhalb von PROJ-45 wäre die Drift, nicht der Gewinn (Konsolidierung samt `path`-Pflichtfeld → **PROJ-Y-45j**). **Q3 → 14 Tage** als Konstante, nicht als Parameter (Nutzer-Lock; ein Parameter vergrössert die Pentest-Matrix ohne Nutzen, solange es keinen Umschalter gibt). **Q4 → optionales Feld neben `readiness`**, exakt dem PROJ-56-ε-Muster folgend (eigener Fehler-Zweig, Guard im Rendering, Abschnitt wird **weggelassen** statt leer gerendert), nur im Status-Report — die Exec-Summary führt `readiness` bewusst nicht; Rückwärtskompatibilität ist **gratis**, weil der Schnappschuss-Inhalt beim Lesen gar nicht validiert wird (keine Zod-Prüfung), und **AC-45δ.20 ist strukturell erfüllt**, weil die Tabelle weder Änderungs- noch Löschregel hat. **Q5 → CSV ja**, aus dem Bestand begründet statt aus Prinzip: δ liefert die Engpass-Sicht, die PROJ-103 für Aufgaben hat — **und die hat einen Export**; ohne δ-CSV könnte derselbe Nutzer überfällige Aufgaben exportieren, überfällige Mängel nicht (α/β/γ haben bisher **keine** Export-Route; die Escaping-Hilfe wird **kopiert**, nicht vereinheitlicht — eine Vereinheitlichung berührte mindestens fünf Dateien und wäre CIA-pflichtig → **PROJ-Y-45k**). **Drei Befunde korrigieren eine Vorlage oder ein Kriterium.** (1) Der in den Anforderungen genannte Funktionsname war falsch — deployt ist **`construction_defect_summary`** (Singular); im Anforderungstext berichtigt, damit `/backend` nicht an einem Namen scheitert, der erst beim Anwenden auffällt. (2) Die naheliegende Umsetzung von AC-45β.18 — Anker-Ersetzung an `project_task_bottlenecks` mit eigenem Schlüssel — ist **abgelehnt**: technisch sauber und fachlich wirkungslos, weil ein Bauprojekt die PROJ-103-Fläche gar nicht erreicht; die Zahl entstünde dort, wo sie niemand sieht, und der PROJ-103-Pentest nagelt absolute Zahlen fest. (3) **AC-45δ.18 (Zusage „byte-identisch") ist heute nicht messbar** — der Schnappschuss-Aggregator hat **keinen** Unit-Test, der Route-Test mockt ihn weg, und **keine** Visual-Baseline fotografiert die Report-Fläche; ein Kriterium ohne Instrument ist eine Behauptung, deshalb bringt δ den Wächter selbst mit (eingefrorene Feldliste plus Abwesenheit des Bau-Schlüssels, rot-grün belegt). Dazu ein vierter, kleinerer Fund: der Aggregator liest heute die **Methode**, nicht den **Projekttyp** — ohne diese Ergänzung ist die Gate-Bedingung des Berichts-Blocks nicht auswertbar. **Die gewichtigste neue Entscheidung ist D-δ4:** das Prädikat für offene Vorbehalte wird ein geteilter SQL-Helfer, und γs **einzige** Inline-Stelle wird per Anker-Ersetzung aus der Live-Definition darauf umgestellt, damit es in SQL genau **eine** Autorität gibt (die TypeScript-Konstante bleibt der Zwilling wie bei β). Die Alternative — Helfer daneben stellen, γ nicht anfassen — ist verworfen, weil dann **drei** Kopien derselben Statusliste existieren und die gefährliche Verwechslung mit der Überfälligkeitsregel unauffällig bleibt. Preis: δ berührt damit einen **deployten Schreibpfad**; das Tor dafür ist der **γ-Pentest wörtlich** plus ein neuer Vektor, der belegt, dass die Protokollier-Funktion nach der Umstellung dieselbe Entscheidung trifft (AC-45δH-10). **D-δ5 weicht bewusst von PROJ-102 ab:** verworfene Arbeitspakete fallen aus dem Nenner, weil ein Fortschritt, der 100 % nie erreichen kann, auf einer operativen Bau-Fläche irreführend wäre; der Phasen-Ersatzweg zählt „abgeschlossen" als erledigt, „verworfen" fällt heraus, „geplant/laufend/ausgesetzt" zählen als offen. Beide Formeln werden mit Grenzfällen eingefroren. **Fünf zusätzliche Härtungskriterien** (AC-45δH-10…14, darunter je Route ein eigener Modul-Gate-Test — α musste das Tor in alle sieben Routen nachziehen — und der **Enkel**-Fall des Teilbaums, der β und γ je einen Vektor gekostet hat) und **sechs benannte Risiken für `/qa`**. Kein neues Paket, eine Migration.  **`/backend` live 2026-08-20:** Migration `20260820180000_proj45_delta_construction_schedule_signals` in Prod (registriert `20260820155944`, Drift benign — durchgängig `create or replace` + idempotente DO-Blöcke). Drei neue Funktionen (zwei Prädikat-Autoritäten `immutable`, die Auswertung `SECURITY INVOKER`/`stable`/`search_path`/ohne Actor-Parameter/`anon` **und PUBLIC** ohne EXECUTE), zwei Anker-Ersetzungen aus der Live-Definition, **keine neue Tabelle, kein Register-Eingriff** (L30 gehalten); die Migration prüft ihr Ergebnis selbst über fünf Funktionen. **D-δ4 nach einer Messung erweitert — zwei Umstellungen statt einer:** live tragen **vier** Funktionen `in_bearbeitung`, davon sind nur zwei Regel-Kopien (`_construction_defect_is_overdue` Paar-Liste, `record_construction_acceptance` Dreier-Liste → umgestellt); `construction_defect_summary` zählt einen Einzelstatus und `transition_construction_defect_status` beschreibt Zustandsübergänge → unberührt. Grund für die Erweiterung: δ braucht für „Mangel **ohne Frist**" die Statuspaar-Liste, und der β-Helfer gibt sie nicht her (er verlangt ein Datum) — ohne den neuen Helfer hätte δ sie ein **drittes** Mal getippt, also genau das, was D-δ4 verhindern soll. **Zwei Entwurfsannahmen korrigiert:** (1) **D-δ2 war zu stark** — „die vorhandenen Auswertungen aufrufen statt nachzuzählen" geht für den Gewerk-Block **nicht**, weil `construction_defect_summary.by_trade` über die Mängel gruppiert und nur Gewerke **mit** Befund listet, während AC-45δ.1 ausdrücklich alle verlangt; δ baut die Liste aus `project_construction_trades` und wiederverwendet die **Prädikate** statt der Gruppierung, Vektor J prüft die überlappende Zahl gegen β. (2) Die abweichenden Schlüsselnamen (β `project_trade_id` / γ `trade_id`) sind **nur Namen** — beide Fremdschlüssel zeigen am Katalog gemessen auf `project_construction_trades.id`. **Live-Pentest `tests/sql/PROJ-45-delta-schedule-signals-pentest.sql` 46/46 PASS gegen Prod, 0 Rückstände** (21+16+9); tragend: **D/D2** die Wurzel zählt die Arbeitspakete des **Enkels** und die naive Ein-Knoten-Abfrage findet 0 (AC-45δH-13) · **E** verworfene fallen aus dem Nenner (50 % statt 33 %, D-δ5) · **G** nichts verknüpft ergibt **kein** „0 %" · **B** eine abgesagte Abnahme blockiert nicht · **H6** der fertiggemeldete Mangel ist keine Frist (β), zählt aber als offener Vorbehalt (γ) — zwei Zahlen, zwei Bedeutungen · **L/L2+M/M2** Aggregat-Leck-Probe mit Gegenprobe in **jeder** Kopfzahl und **jeder** Liste · **K2** der Leser ist nachweislich kein Admin (sonst falsch-grün) · **Q/Q2/R/R2** die Parität der Umstellung auf **beiden** Seiten, inklusive „nach `geprueft` löst sich der Blocker ohne Zutun" · **P/P2** Verhaltenstabellen statt Textprüfung. Beim ersten Lauf gefangen: `phases.sequence_number` ist NOT NULL. **Regressionen wörtlich: β 53/53 · γ 60/60 · α 18/18 · PROJ-Y-45a 9/9 · PROJ-103 7/7**, je 0 Rückstände und 0 deaktivierte Trigger; bei γ ist zusätzlich belegt, dass die Umstellung in Prod **wirklich aktiv** ist (Helfer vorhanden, Aufruf im Rumpf) — sonst hätte „60/60 grün" auch „nichts umgestellt" bedeuten können. **Ein Befund an der Nahtstelle, den kein Typ gefangen hätte:** die Datenroute antwortet `{ signals: … \| null }`, der erste Client-Wrapper las die nackte Nutzlast und hätte in Produktion ein Objekt aus lauter `undefined`-Feldern geliefert (beide Seiten tragen nur `as`-Zusicherungen); korrigiert, drei Tests pinnen die Hülle, und bewusst **kein** erfundenes Leer-Objekt, weil ein ausgedachtes `as_of` genau auf der Fläche falsch wäre, die „nichts da" von „0" trennt. **Der Wächter für AC-45δ.18 existiert jetzt und beisst** — zuvor war die Zusage „byte-identisch" unmessbar (kein Aggregator-Test, Route-Test mockt ihn weg, keine Visual-Baseline); rot-grün in **drei** Richtungen, darunter die Erkenntnis, dass die `in`-Prüfung ohne Umsortierung nie nachweislich wirksam war. Neue Entscheidungen D-δ11…D-δ16 (Überfälligkeit bei Arbeitspaketen folgt PROJ-103 wörtlich · `linked_count` neben `source_count`, damit „verknüpft aber nichts zählbar" unterscheidbar bleibt · Berichts-Block als Auswahl · unbekannte CSV-Sektion → 400 statt Default · kein Zugriffs-Protokoll, weil Bau keine Vertraulichkeitsstufe trägt · `manual_status` nutzt den α-Typ statt einer vierten Kopie). Gates: vitest **3469/3469** · ESLint 0 · tsc 13 = Baseline/0 neu (auch nach dem Build) · Build clean mit beiden Routen · migration-naming 0 · index-scope 0 · Funktions-Inventar **283 → 286** gegengezählt · Advisors **149 WARN / 0 ERROR**, keine davon zu den drei δ-Funktionen.  **`/frontend` live 2026-08-20:** Reiter „Terminsignale" als **fünfte** Bau-Fläche hinter demselben **einen** Modul-Schalter — Kopfzeile mit den vier getrennten Zahlen, Gewerke mit **manueller α-Ampel und gerechnetem Signal nebeneinander** und benannten Gründen, Bauabschnitte als eingerückter Baum mit **Quellenangabe statt „0 %"**, nächste Fristen (verstrichene oben, gekennzeichnet), Engpass-Sicht, CSV je Block; **rein lesend**, jede Aktion ein Sprung, und bewusst **kein** `manage_members`-Gate (das ist γs Schreib-Gate). Keine Migration, kein Paket, kein Backend-Diff. **Der Navigations-Eintrag kam ohne Testanpassung dazu** — α hatte die Registry-Invariante damals auf ihre **Absicht** umgestellt, das zahlt sich zum dritten Mal aus (125 Fälle und die fünf Projekttyp-Filter-Fälle unberührt, `requiresProjectType` bleibt einwertig). Berichts-Block **hinter den Meilensteinen, vor den Risiken**, per Guard ganz weggelassen statt leer gerendert; **D-δ6 doppelt erledigt** (Bau-Block **und** der von PROJ-56-ε nie eingetragene `readiness`-Block stehen jetzt in der „locked order"). **D-δ-FE-1:** der Bericht ruft `describeProgressSource` **nicht** — die Funktion braucht die drei Mengenangaben, die der eingefrorene Block bewusst weglässt, und mit Platzhalter-Nullen zu rufen hiesse Zahlen behaupten, die im Schnappschuss nie standen; die zwei Null-Fälle bleiben unterschieden und „0 %" erscheint nie, wo nichts gemessen wurde. **Zwei eigene Fehler, von den Gates gefangen:** die erste Auth-Gate-Zusicherung prüfte auf `overdue_defects` — ein **Sektionsname**, der im gespiegelten `?next=` steht, also in der Eingabe des Aufrufers; genau diese Verwechslung warnt der γ-Spec im Kommentar an. Und drei ESLint-Fehler an deutschen Schlusszeichen im JSX, behoben mit der **typografischen** Form statt `&quot;`. **Ein Fund ausserhalb der Slice, der jede lokale Messzahl berührt:** im Repo-Wurzelverzeichnis liegt seit diesem Abend ein fremdes, untracked Projekt (`U-Know/`, **4,1 GB**); weil `tsconfig.json` `**/*.ts(x)` einzieht, meldet `tsc` lokal 4632 statt 13 Fehler, `npm run build` schlägt in der Typprüfung fehl und `vitest` meldet 86 fehlgeschlagene Dateien — **alles** aus jener Fläche. Der Build wurde deshalb in einem **frischen git-Worktree** auf demselben Commit gemessen: tsc **13 = Baseline**, Build **clean** mit allen drei Flächen registriert. Das Verzeichnis ist nicht committet, erreicht CI also nicht, und wurde bewusst nicht angefasst; dauerhaft gehörte es in `.gitignore` **und** `tsconfig.exclude` — Entscheidung des Repo-Eigners. Nachweise: **Auth-Gates δ 6/6** chromium · **Visual-Regression 9/9 ohne Neuaufnahme** (gemessen, nicht geschlossen — die Sektion ist projekttyp- **und** modul-gegatet, der Visual-Mandant hat beides nicht) · α/γ-Auth-Gates unverändert grün · **vitest 3484/3484 in 415 Dateien** (ohne `U-Know/`) · ESLint **0**. **Eine Regression, die nicht δ gehört:** β's authentifizierte Spec fällt mit **5 failed / 1 did not run / 12 passed**, Stacktrace auf `removeRunDefects → deleteOrThrow` — byte-identisch zur bereits als **PROJ-Y-45h** registrierten Signatur (Ursache PROJ-Y-148d); strukturell belegt, dass δ es nicht sein kann, weil die δ-Migration **0** DDL-Anweisungen enthält und `construction_defects` nur zweimal **lesend** nennt. Abweichungen D-δ-FE-1…FE-4, darunter **kein authentifizierter Browser-Durchlauf in diesem Schritt** (Modul im geteilten Mandanten einzuschalten hätte die frisch gemessenen Visual-Baselines verschoben) → `/qa` mit der Bau-Fixture-Lane, die live gemessen **von sich aus nur den Leerzustand** hergibt (1 Gewerk ohne Befund, 2 Abschnitte ohne Verknüpfung, 0 Fristen, 0 Engpässe) — ein echter Blocker muss dort geseedet werden, sonst prüft der Durchlauf nur die leere Hälfte.  **`/qa` PASS 2026-08-21 (0 Critical / 0 High / 0 Medium → Approved):** 24/24 AC, 14/14 Härtungskriterien, kein offenes Kriterium. **Der Kern ist der Durchlauf, den `/frontend` offen gelassen hatte — mit geseedetem Blocker:** die Fixture-Lane gibt von sich aus nur den Leerzustand her, ein Durchlauf darauf hätte von den vier Blöcken **keinen** im gefüllten Zustand belegt. `tests/PROJ-45-delta-signals-chain.spec.ts` **3× 2/2** prüft beide Hälften gegeneinander (Blocker mit benanntem Grund, drei getrennte Zahlen, „aus 2 Arbeitspaketen im Teilbaum" an der **Wurzel** obwohl die Pakete am Kind hängen, 50 %, verstrichene Frist, Engpass-Zeile — gegen einen Abschnitt ohne Verknüpfung, der weder Balken noch „0 %" zeigt). **Zwei negative Zusicherungen tragen den Rechte-Teil:** der Betrachter sieht die Fläche vollständig (keine verschärfte Rolle, anders als γ) und bekommt **keinen** Schreibweg — während derselbe Betrachter im **gleichen** Test auf der Mängel-Fläche „Mangel erfassen" sehr wohl sieht (β/L15); ohne diese Gegenprobe belegte „kein Knopf" nur die Abwesenheit eines Knopfes. **Live gegen Prod 199 PASS / 0 FAIL über sechs Dateien, 0 Rückstände:** δ **52/52** (21+16+9+2+4) · δ-Rot-Team **21/21** (neu) · β **53/53** · γ **60/60** · α **18/18** · PROJ-Y-45a **9/9** · PROJ-103 **7/7** wörtlich (die absoluten Zahlen halten). **0** deaktivierte Trigger mit Gegenprobe „19 vorhanden, 19 aktiv"; die 14 Bestands-Mängel im Bau-Mandanten sind der β/γ-Fixture **zugeordnet** statt unerklärt gelassen. Das **Tor** der Slice ist belegt statt bloss grün: beide Umstellungen in Prod aktiv (je 1 Aufruf im Rumpf, altes Literal im β-Helfer verschwunden, γ-Geschwister-Zweige erhalten), beide Helfer `immutable` mit `search_path`, `anon` **und PUBLIC** ohne EXECUTE. **Drei Lücken hat `/qa` selbst geschlossen:** (1) AC-45δ.2 und AC-45δ.15 waren **nur Code** — keine Testdatei rendert die Fläche; neu **19 Komponenten-Fälle** in drei Dateien, jeweils **rot-grün belegt**, der schärfste mit einer **absichtlich widersprüchlichen** Vorlage (Zusammenfassung 7/5/3/2 gegen Listen mit einer Zeile) — nur eine Kopfzeile, die aus `summary` liest, kann das anzeigen, und die Summe 15 darf nicht vorkommen; dabei fiel auf, dass „Überfällig" zweimal im DOM steht (Kachel und Gewerk-Zähler), der Zugriff ist entsprechend präzisiert. (2) Die **Aggregat-Leck-Probe deckte vier von sechs** Kopfzahlen — neuer Block 2b seedet so, dass alle drei Mangel-Zähler ungleich 0 sind (sonst trivial erfüllt) und **iteriert über die Schlüssel**, damit ein künftiger siebter Zähler nicht stillschweigend ungeprüft bleibt. (3) **AC-45δ.20 hing an einem Strukturargument** — neuer Block 4 macht daraus Verhalten (**42501** auf UPDATE **und** DELETE, und S3 belegt, dass die Zeile für denselben Nutzer lesbar ist, sonst wären beide trivial grün). **Befunde:** F-δ1 (Low, behoben) der CSV-Knopf trug als zugänglichen Namen nur den Blocknamen, jetzt „… als CSV herunterladen" · **F-δ2 (Low, offen → PROJ-Y-45l)** der Rekursions-Riegel `depth < 20` **unterberichtet still**: bei 25 Ebenen zählt die Wurzel nur bis 20, der tiefe Knoten sieht sein eigenes Paket — kein Fehler, kein Hängen, aber der Fortschritt an der Wurzel ist zu niedrig, ohne dass die Fläche es sagen könnte (reale Bäume haben 2–4 Ebenen) · **F-δ3 (Info, offen → PROJ-Y-45m)** die Auswertung joint `projects` nicht und filtert `is_deleted` nicht, Zeilen eines weich gelöschten Projekts erscheinen bei bekannter Kennung — kein Rechteproblem und **konsistent mit den Geschwister-Flächen**, daher cross-cutting statt δ-Defekt · **F-δ4 (Info) korrigiert ein registriertes Followup: PROJ-Y-45h ist enger als notiert** — ein Mangel ist nicht grundsätzlich unlöschbar, sondern erst **mit** Verlaufszeilen; die fünf Zeilen dieses Laufs gingen mit einem gewöhnlichen `DELETE`, ohne Runbook-Weg und mit 0 deaktivierten Triggern, was β's Teardown bricht ist die unveränderliche **Historie** · F-δ5 (Info, eigener Fehler) zwei meiner Zusicherungen waren falsch, nicht das Produkt: die Auth-Gate-Marke `overdue_defects` ist ein Sektionsname und steht im gespiegelten `?next=` (genau die Verwechslung, vor der der γ-Spec warnt), und die Quellenangabe hatte ich erfunden statt nachgelesen. Gates: vitest **3496/3496** in 419 Dateien · ESLint 0 · tsc 13 = Baseline/0 neu · Playwright **36/36** · **Visual 9/9 ohne Neuaufnahme** · Advisors 149 WARN / **0 ERROR**, keine zu den drei δ-Funktionen · index-scope 0 · migration-naming 0 · Funktions-Inventar 286 gegengezählt. Alle lokalen Zahlen ohne das fremde `U-Know/` gemessen. Abweichungen D-δ-QA-1…3 (Mobile-Safari-Env; β's authentifizierte Spec fällt weiter im Teardown = PROJ-Y-45h, strukturell nicht δ; Rot-Team auf DB-Ebene). Offen: `/deploy`. |
| PROJ-45-ε | Planned (Followup, 45) | **Fotodokumentation** am Mangel über das DMS (PROJ-79, inkl. Vertraulichkeits-Gate aus PROJ-Y-115c). Nutzer-Lock L4 hat sie bewusst aus α herausgehalten; die V2-Quelle nennt sie als Kernkompetenz, sie zielt aber auf β. |
| PROJ-Y-45a | **Resolved 2026-08-14** — Tag `v2.57.0-PROJ-Y-45a`, PR #390 | **Erledigt.** Zwei BEFORE-Wächter (`work_item_construction_reference_guard`, `risk_construction_reference_guard`) erzwingen, dass ein referenziertes Gewerk bzw. ein Bauabschnitt zum **selben Projekt** gehört. Zwei Funktionen statt einer, weil `risks` keine `section_id` hat und ein gemeinsamer Rumpf die Spalte über `to_jsonb(NEW)` erraten müsste. Auf `UPDATE OF <spalten>` eingegrenzt, mit frühem Ausstieg wenn beide Verweise leer sind — der Normalfall jedes Nicht-Bauprojekts; die Bauform spiegelt `work_items_validate_release`. Post-Condition prüft den Bestand und bricht laut ab statt eine Inkonsistenz zu behalten. **Live-Smoke 9/9 gegen Prod, 0 Rückstände** (`tests/sql/PROJ-Y-45a-reference-consistency-smoke.sql`): R4/R4b/R4c (UPDATE Gewerk, UPDATE Abschnitt, INSERT projektfremd) alle **23514**, R5a/R5b dasselbe für Risiken — und ebenso wichtig die N-Vektoren N1/N2/N3/N4, die belegen, dass Anlage, korrekte Zuordnung, gewöhnliche Bearbeitung und das Entfernen von Verweisen unbeschädigt sind. Advisors 0 ERROR und **kein neuer WARN**, weil beide Wächter für `anon`/`authenticated` entzogen sind.
| PROJ-Y-45b | Planned (Followup, 45) | **Die zwei projektbezogenen α-Entfernen-Pfade bilden jeden Datenbankfehler ausser `42501` auf 500 ab.** PROJ-45-β hat das für **Mängel** behoben (`23503` → 409 mit Nennung der blockierenden Mängel, für Abschnitte über die Teilbaum-RPC, weil ein flacher Filter genau den Enkel-Fall verfehlt). Für **künftige** Verweise auf `project_construction_trades` und `construction_sections` bleibt der rohe 500-Pfad bestehen: wer als nächster eine Sperre an diese Achsen hängt (γ Abnahmen, ε Fotos), muss den 409-Zweig erneut ergänzen. Sauber wäre eine gemeinsame Abbildung `23503` → 409, die ihre Blockierer über einen Registrierungspunkt einsammelt, statt sie je Aufrufstelle aufzuzählen. Kein Sicherheitsbefund, reine Meldungsqualität. |
| PROJ-Y-45c | Planned (Followup, cross-cutting) | **Prod/Repo-Divergenz der Klasse PROJ-Y-130f, in PROJ-45-β belegt.** `_project_teardown_active()` existiert in Prod und wird von `enforce_deliverable_approval_event_immutability` benutzt, aber **keine** Migrationsdatei erzeugt es (nachgezählt: 0 Treffer in `supabase/migrations`) — eine frisch aus den Dateien gebaute Datenbank hat weder den Helfer noch die entschärfte Trigger-Fassung; die deployte Datei `20260703130000_proj105_deliverable_approvals.sql` enthält die **strikte** Variante. β hat sich deshalb bewusst **nicht** darauf gestützt und seine Ausnahme selbsttragend formuliert (der Mangel existiert nicht mehr), sonst hätte der Schema-Drift-Wächter die Migration gebrochen. Zu klären: fix-forward-Migration, die Helfer und Triggerfassung in den Dateien nachzieht, damit Prod und Repo wieder übereinstimmen. Kein Sicherheitsbefund; die Wirkung ist, dass eine frische Umgebung das Projekt-Teardown-Verhalten von PROJ-105 nicht hat. |
| PROJ-Y-45d | Planned (Followup, 45-β) | **Der Gewerk-Auswahlliste im Mangel-Dialog fehlt ein kontrollierter Anfangswert.** `construction-defect-dialog.tsx:241` übergibt `value={draft.trade_id.length > 0 ? draft.trade_id : undefined}`; beim Öffnen der Erfassen-Maske ist das Feld leer, React behandelt die Komponente daher als *unkontrolliert* und kippt bei der ersten Auswahl auf *kontrolliert* — die Konsole meldet „Select is changing from uncontrolled to controlled". In `/qa` bei jedem Öffnen der Maske reproduziert (PROJ-45-β, F-2). **Es ist die einzige Stelle dieser Form im ganzen Repo** (nachgezählt: 1 Treffer für `value={…length > 0 ? … : undefined}` in `src/`), also von β eingebracht und nicht aus dem Bestand geerbt — die beiden optionalen Auswahllisten unmittelbar darüber (Ort, Nachunternehmer) machen es mit einem `NONE`-Wächter richtig. Wirkung ist Konsolenlärm auf einer neuen Fläche, keine Fehlfunktion: die Auswahl arbeitet korrekt (Playwright 18/18 über genau diesen Pfad). Bewusst **nicht** in `/qa` behoben, weil der naheliegende Fix (`value={draft.trade_id}`) das Verhalten des Platzhalters „Gewerk wählen …" berührt und dessen Prüfung zum Frontend gehört. Nebenbefund: der Konsolen-Wächter aus PROJ-Y-143e greift auf `console.error`, diese Meldung ist eine `console.warn` — er konnte sie also nicht fangen. |
| PROJ-Y-45e | Planned (Followup, cross-cutting) | **`tenants.audit_lifecycle_exempt` deckt den FELD-Audit nicht ab — ein committender E2E-Durchlauf über eine Zustandsmaschine lässt den append-only Trail dauerhaft wachsen.** In PROJ-45-β `/qa` gemessen: drei Durchläufe des Zwei-Personen-Durchlaufs hinterließen **33** unlöschbare Zeilen in `audit_log_entries` (28 × `construction_defects.status`, 5 × `section_id`) plus eine aus dem Fixture-Seed (`tenant_settings.active_modules`). Das Flag aus PROJ-Y-130h unterdrückt ausdrücklich nur `record_audit_lifecycle` (Anlage/Löschung), nicht `record_audit_changes` — `status` steht in der Feld-Whitelist, weil AC-45β.7 die Auditierung jedes Wechsels verlangt, und seit PROJ-130-α gibt es keinen Löschpfad. `/qa` hat die Zeilen über den Runbook-Weg (`session_replication_role = replica`, streng auf den Fixture-Mandanten begrenzt, mit Vorbedingungen und Nachprüfung) wieder entfernt und 0 Rückstände belegt — aber das ist ein manueller Nachlauf, den der nächste Lauf erneut braucht. Zu entscheiden: entweder das Ausnahmeflag auf den Feld-Audit ausweiten (dann ist es kein reines „lifecycle"-Flag mehr und braucht einen neuen Namen), oder einen sanktionierten, mandantengebundenen Aufräumschritt bereitstellen, den ein Spec-Teardown selbst aufrufen darf. Kein Sicherheitsbefund; die Wirkung ist Testrauschen in einem Compliance-Artefakt, genau die Klasse, die PROJ-Y-143o/130h benannt haben. |
| PROJ-Y-45f | Planned (Followup, 45-γ) | **`construction_section_blocking_defects` ist nach dem γ-Deploy ohne Aufrufer.** γ hat die Entfernen-Meldung aus α/β verallgemeinert (neuer Code `references_present`, zwei INVOKER-Auskünfte `construction_trade_blocking_refs` / `construction_section_blocking_refs`, die **Art und Bezeichnung** des Blockers nennen) — nötig, weil die alte Meldung wörtlich „Mängel" sagt und mit Abnahmen **falsch** statt bloss unvollständig geworden wäre. Die β-Funktion bleibt damit als toter Code in Prod stehen. Keine Sicherheitswirkung, reine Hygiene; das Ziehen gehört in eine eigene Migration mit Post-Verifikation, dass kein Aufrufer übersehen wurde. |
| PROJ-Y-45g | Planned (Followup, 45-γ) — **Auslassung aus AC-45γ.24** | **Beleg als Dokumentknoten auswählbar machen.** AC-45γ.24 verlangt „entweder eine externe Adresse **oder** ein vorhandener Dokumentknoten aus dem DMS (PROJ-79)". **Die Datenbankseite ist vollständig und geprüft** — `document_node_id` existiert, der Einzel-Beleg-CHECK schliesst beides-zugleich aus, der Rot-Team-Vektor **S** belegt, dass ein **fremder** Dokumentknoten mit `23514` abgewiesen wird. Es fehlt allein der **Picker** in der Oberfläche; über die Maske ist heute nur der externe Weg erreichbar. Damit ist das Kriterium serverseitig erfüllt und für den Nutzer halb — deshalb hier als Auslassung registriert statt als erledigt gebucht. Fix = DMS-Knoten-Auswahl im Beleg-Dialog (Muster: der Knoten-Picker aus PROJ-77-γ). |
| PROJ-Y-45h | Planned (Followup, 45-γ) | **Teardown für die Bau-E2E-Lane.** γs Abnahmen und β's Mängel sind **by design** nicht mehr löschbar — richtig für ein Rechtsprotokoll, der vorgesehene Weg für einen Irrtum ist „absagen" statt löschen. Folge: die authentifizierten Specs beider Slices können sich nicht selbst aufräumen; β's Spec fällt heute in ihrem `afterAll` (gemessen: 5 failed / 1 did not run / 12 passed, Stacktrace auf `deleteOrThrow → removeRunDefects`), und γs Kette hinterlässt Zeilen, die nur über den Runbook-Weg (`session_replication_role = replica`, `docs/production/prod-test-fixtures.md`) entfernbar sind. Gebraucht wird ein **sanktionierter, mandantengebundener** Aufräumschritt für die Bau-Fixture-Lane (Klasse PROJ-Y-45e / PROJ-Y-130h). **Schliesst zugleich die zweite Hälfte von F-γ4** — β's Teardown ist dieselbe Lücke, nur eine Slice früher. |
| PROJ-Y-45i | Planned (Followup, 45-γ) | **Verdeckter Titel im Ladezustand der Abnahme-Detailansicht** (QA-Befund F-γ2, Low). Während die Detailansicht lädt, trägt die Fläche keinen zugänglichen Namen — für Screenreader ist der Kontext in diesem Fenster verloren. Kein Datenleck, keine Fehlfunktion; WCAG-Hygiene. Fix = Titel schon im Ladezustand rendern statt erst nach dem Eintreffen der Daten. |
| PROJ-Y-45j | Planned (Followup, 45-δ) | **Der `ltree`-Pfad der Bauabschnitte ist angelegt, indexiert und ungenutzt.** α legt `construction_sections.path` als echtes `ltree` **mit GiST-Index** an, setzt ihn per BEFORE-Trigger bei jedem Insert (Label ist die UUID, von Umbenennungen also unberührt) und hängt Teilbäume beim Verschieben per AFTER-Trigger um; β und γ fragen den Teilbaum trotzdem **rekursiv über `parent_id`** ab, δ folgt ihnen (D-δ3). Damit koexistieren im Repo drei Techniken für dieselbe Frage (`<@` in PROJ-36 und den α-Triggern, rekursive CTE in β/γ/δ, Text-Präfix über PostgREST in der Work-Items-Route). Aufzuräumen wäre: `path` per Fix-forward auf `not null` (heute `nullable` **ohne** CHECK, live 0 Verstösse) und die drei Teilbaum-Abfragen auf `<@` mit qualifiziertem Operator vereinheitlichen. Betrifft α, β, γ **und** δ, gehört also nicht in eine Lesefläche; Nutzen ist Konsistenz und Indexnutzung, nicht Perf — die Bäume sind winzig. |
| PROJ-Y-45k | Planned (Followup, cross-cutting) | **Die CSV-Escaping-Hilfe existiert in sieben Kopien.** Jede der sieben projektbezogenen Export-Routen (PROJ-103/111/113/119/122/131/132) definiert dieselbe Formel-Neutralisierung neu — δ macht die achte (D-δ7, bewusst kopiert). Eine Vereinheitlichung in eine geteilte Hilfe berührt mindestens fünf Dateien und ist damit **CIA-pflichtig**; sie muss ausserdem zuerst belegen, dass die sieben Kopien wirklich identisch sind, sonst wird eine Abweichung still eingesammelt. Kein akutes Risiko: alle Kopien neutralisieren die vier Formel-Präfixe und verdoppeln Anführungszeichen. |
| PROJ-Y-45l | Planned (Followup, 45-δ) | **Der Rekursions-Riegel der Terminsignal-Auswertung unterberichtet still.** `construction_schedule_signals` begrenzt den Abschnitts-Teilbaum auf `depth < 20`; im Rot-Team-Lauf mit **25** verschachtelten Abschnitten zählt die Wurzel nur bis Ebene 20 (`subtree_depth=20`, `linked_count=1`), während der tiefe Knoten sein eigenes Arbeitspaket sehr wohl sieht. Kein Fehler, kein Hängen — aber der Fortschritt an der Wurzel ist zu niedrig, **ohne dass die Fläche das sagen könnte**, und genau diese Klasse (stille Unterberichtung) ist im Haus als Defekt geführt. Der Riegel war eine defensive Zutat: Zyklen sind durch `construction_sections_no_self_loop` und den `<@`-Zyklustest im α-Wächter strukturell ausgeschlossen. Drei Wege: Riegel entfernen (die DB verhindert Zyklen ohnehin) · Riegel anheben und die Kappung im Ergebnis **ausweisen**, damit die Oberfläche sie benennen kann · so lassen und die Grenze dokumentieren. Reale Abschnittsbäume haben 2–4 Ebenen, die Fixture zwei — daher Low und nicht dringend, aber zu entscheiden statt zu vergessen. |
| PROJ-Y-45m | Planned (Followup, cross-cutting) | **Weich gelöschte Projekte werden von den Bau-Auswertungen nicht ausgeblendet.** `construction_schedule_signals` joint `projects` gar nicht und filtert deshalb `is_deleted` nicht: wer die Projekt-Kennung kennt, erhält die vollständige Auswertung eines Projekts im Papierkorb (Rot-Team-Befund R4c). **Kein Rechteproblem** — RLS und Projekt-Mitgliedschaft entscheiden unverändert, und die Navigation verlinkt Papierkorb-Projekte nicht. Der Punkt ist Konsistenz: zu prüfen ist, ob die **Geschwister**-Flächen (Mängel-, Abnahme-, Engpass- und Berichts-Auswertungen) dasselbe tun — dann ist es eine produktweite Konvention und gehört als solche entschieden und dokumentiert, nicht in einer Slice einseitig geändert. |
| PROJ-Y-1 | **Umbenannt 2026-08-18 — fortgeführt als [PROJ-Y-114a](#) in dieser Datei** | Der Name `PROJ-Y-1` war mehrfach belegt (PROJ-98, PROJ-114, PROJ-116 und diese globale Zeile). Die Sache selbst — Quelle/Herkunftsnachweis am DD-Finding — steht als **PROJ-Y-114a**. Diese Zeile bleibt als Verweis stehen, damit ältere Notizen auffindbar bleiben; Sammel-Bereinigung der Kollision: **PROJ-Y-145h**. |
| PROJ-Y-2 | **Deployed 2026-08-18 (FE-only) — Scope `full`** | **Ausgeliefert:** PR #397 gemergt (`9a2d59e`, 14:00:20Z), Tag `v2.60.0-PROJ-Y-2` auf dem Merge-Commit; Vercel-Produktions-Deploy `dpl_DV6hjAaiW47zY13yPji2pTRBQchD` aus genau diesem SHA **READY**, 0 Laufzeitfehler danach. **Korrektur einer Falschaussage in dieser Zeile:** hier stand bis zum Abschluss „In Review 2026-08-17 — gebaut, PR offen" und „Gebaut, noch nicht deployed (Scope bleibt daher `—`)", während der PR bereits gemergt war — Register, INDEX und Spec liefen also auseinander. Alle acht Akzeptanzkriterien sind beim Abschluss einzeln am Code auf `main` gegengelesen (nicht aus der Nachweis-Spalte übernommen), nichts ist zurückgestellt, kein Critical/High offen → `full`. Spec: [PROJ-Y-2](PROJ-Y-2-red-flag-lens.md). Umschalter Alle/Red Flags auf dem DD-Findings-Panel mit Zählern, linsenabhängiger EUR-Summe (SteerCo-Schnellsicht) samt Angabe der Befunde **ohne** Schätzung, Report-Parität in der Sortierung, leerer Zustand, Hinweis bei Abweichung zwischen unbeschränktem Aggregat und der auf 500 Zeilen gedeckelten Liste. Kein Backend, keine Migration, keine Dependency — 3 Dateien, keine davon serverseitig. **Die Definition ist abgeleitet statt erfunden:** aus der WHERE-/ORDER-Klausel des deployten `dd_report_consolidated`, am 2026-08-17 aus `pg_get_functiondef` gegen Prod gelesen — `severity in ('hoch','deal_breaker')`, `order by (severity = 'deal_breaker') desc, economic_impact_eur desc nulls last`, **kein Status-Filter** (ein erledigter Deal Breaker bleibt im Report gelistet). **Eine Präzisierung gegenüber der hier zuvor notierten Fassung:** PROJ-108 formulierte `severity ≥ hoch` als Ordinalvergleich; das fällt bei den heutigen vier Werten mit der Aufzählung zusammen, ist aber die schwächere Zusage — ein künftiger fünfter Schweregrad fiele stillschweigend hinein oder heraus. Umgesetzt ist die Aufzählung, erschöpfend über alle Schweregrade getestet. **Kein Aggregat-Leck, gegen Prod verifiziert:** `dd_findings_summary` und `dd_report_consolidated` beide `prosecdef = false` (SECURITY INVOKER), beide Routen session-gebundener Client plus `requireProjectAccess("view")` ohne Service-Role — und die Slice fügt **gar keine Abfrage hinzu**, sie rechnet auf dem bereits geladenen State. Ein Filter hinter dem Tor, kein zweites Tor. **Rot-Grün-Gegenprobe:** eingeschmuggelter Status-Filter → 1 Test rot, entfernter Deal-Breaker-Vorrang → 2 Tests rot, danach zurückgesetzt, wieder 16/16. Gates: ESLint 0 · tsc 13 = Baseline / 0 neu · vitest 3057/3057 · Build clean · index-scope grün. Gates beim Abschluss erneut gemessen: ESLint 0 · tsc 13 = Baseline / 0 neu · vitest **3058/3058** in 385 Dateien · Build clean · index-scope 173 Zeilen / 0 errors. **Keine zurückgestellte Original-Anforderung** — deshalb steht diese Zeile hier nur noch als Nachweis, nicht als Schuld. D-Y2.1 (kein authentifizierter Playwright-Durchlauf; Fläche modul- **und** projekttyp-gegatet, E2E-Mandant hat kein M&A-Projekt mit Streams; es gibt keine neue Route, die bestehenden `dd-findings`-Routen sind durch PROJ-114 auth-gate-getestet — ein leerlaufender Test wurde bewusst nicht committet) ist beim Abschluss an der AC-Tabelle gegengeprüft und als Abweichung in der **Nachweistiefe** eingeordnet: keines der acht Kriterien verlangt einen Browser-Durchlauf. Die verbleibende, bewusst benannte Lücke — die gerenderte Oberfläche wurde nie angemeldet beobachtet — braucht keine Test-Ergänzung, sondern eine Fixture und ist als **spätere Erweiterung PROJ-Y-2a** registriert. Ursprüngliche Registrierung: „filter/tab for `severity ∈ {hoch, deal_breaker}` + EUR sum (SteerCo quick-view). No backend/migration/dep; pilot-driven. The only genuine net-new of the superseded PROJ-108. From CIA 2026-06-26." |
| PROJ-Y-2a | Planned (spätere Erweiterung, Y-2) | **E2E-Fixture für M&A-Flächen: Mandant mit `project_type='ma'` und aktivierten DD-Streams.** Herkunft: PROJ-Y-2 D-Y2.1. **Ausdrücklich keine zurückgestellte Anforderung** — kein Akzeptanzkriterium von PROJ-Y-2 verlangt einen Browser-Durchlauf, alle acht sind am Code belegt, PROJ-Y-2 ist `full`. Offen ist allein, dass die gerenderte Red-Flag-Linse nie in einer angemeldeten Sitzung beobachtet wurde, und der Grund ist strukturell: die Fläche ist modul- **und** projekttyp-gegatet, der geteilte E2E-Mandant hat kein M&A-Projekt mit Streams, und ein Auth-Gate-Spec wäre leerlaufend (es gibt keine neue Route; die `dd-findings`-Routen sind durch PROJ-114 abgedeckt). Der Weg ist deshalb eine Fixture, nicht ein Test — Muster **PROJ-Y-144d**, das für die Assistant-Fläche einen eigenen Mandanten mit aktivem Modul und methodenbehaftetem Projekt geseedet hat. Nutzt mehreren M&A-Flächen zugleich (Findings-Panel, Stage-Gates, DD-Bericht, Operatives/Steering-Reporting), weshalb es bewusst nicht in eine einzelne Feature-Slice gehört. Ungeplant, pilot-/bedarfsgetrieben. |
| PROJ-Y-114a (vormals `PROJ-Y-1`) | **In Progress 2026-08-18 — gebaut + live-pentestet, QA offen** | **Umbenannt, weil `PROJ-Y-1` fuenffach belegt ist** (PROJ-70 OCR · PROJ-98 Stage-Gate · PROJ-100c Pro-Objekt-sensibel · PROJ-114 E-Mail-Eskalation · PROJ-116 Word-Export) — und ausgerechnet mit **PROJ-114s eigenem** PROJ-Y-1 kollidierte, obwohl beide dasselbe Elternteil haben. Neue ID `PROJ-Y-114a` (kein `PROJ-Y-114*` existierte); `Next Available ID` unberuehrt. **Prior-Art-Pruefung gegen den deployten Stand hat den Umfang halbiert:** der *externe* Dokumentenverweis ist seit **PROJ-115 bereits erfuellt** — `external_document_links_entity_type_check` traegt live `'dd_finding'` und `<ExternalLinksSection>` haengt im Finding-Dialog (`dd-findings-panel.tsx:503`), Regression A–I 9/9 verbatim gruen. Echt gefehlt hat die **Quelle als Aussage**: `dd_findings` hatte 17 Spalten und keine fuer Herkunft, einziger Ablageort war `description` — Befund und Nachweis im selben Freitext. **Gebaut** (Migration `20260817120000` + Fix-forwards `20260817123000`/`20260817124000`, alle in Prod): `source_kind` (CHECK, 6 Werte) · `source_ref` (≤500) · `source_dd_question_id` (FK → `dd_questions`, **ON DELETE RESTRICT**, weil ein Nachweis das Loeschen seiner Quelle nicht stillschweigend ueberleben darf) + Projekt-Konsistenz-Waechter + Need-to-know-Pruefung auf der referenzierten Frage (sonst waere der Verweis ein Existenz-Orakel auf `strict`-Fragen, PROJ-120-F-1-Klasse) + `clear_source` (ohne den Schalter waere eine gesetzte Quelle **nie entfernbar**, PROJ-122-D-1-Defekt) + `_tracked_audit_columns` 10 → **13** per Anker-Ersetzung aus der Live-Definition. **Absorbiert PROJ-Y-113c** (siehe eigene Zeile) und aktiviert dabei den seit sieben Wochen unwahren deaktivierten Platzhalter „Zu Finding eskalieren" (Tooltip „Verfuegbar mit DD-Findings (PROJ-114)", waehrend PROJ-114 seit dem 2026-06-26 live ist). **Live-Pentest A–P 16/16 PASS, 0 Rueckstaende** (10 Zaehlungen; Nicht-Admin synthetisiert, weil in Prod jedes Mandanten-Mitglied Admin ist und `can_access_classified` fuer Admins kurzschliesst → Smoke unter Admin waere falsch-gruen); Regressionen verbatim: PROJ-114 A–J 10/10 · PROJ-115 A–I 9/9 · PROJ-Y-122a-Audit-Smoke 4/4 (`admin_shortcircuit=f`). Advisors 0 ERROR — der Lauf **fand einen eigenen Fehler**: der Waechter war fuer `anon` ausfuehrbar, weil Postgres neuen Funktionen `EXECUTE` an **PUBLIC** gibt und ein Entzug nur auf `anon`/`authenticated` den PUBLIC-Eintrag nicht entfernt (ACL blieb `=X/postgres`); fix-forward entzogen, Gegenprobe: Trigger feuert weiter (Vektor N). **Zweiter Selbstfund (F-4, behoben):** die Freigabe-Pruefung auf der verknuepften Frage lief unbedingt, obwohl der Dialog den Verweis bei jedem Speichern mitsendet — eine Leitung mit Freigabe unter der Stufe der Frage waere von jeder Bearbeitung ausgesperrt worden. Jetzt nur bei echter Aenderung (`is distinct from`); Vektorpaar O/P belegt, dass das Leck-Tor dabei zu bleibt. Gates: ESLint 0 · tsc 13 = Baseline/0 neu · vitest **3055/3055** · Build clean · migration-naming 0 · index-scope 0. **Kein `Deployed`-Stempel und kein Scope**, solange kein QA-Verdikt existiert. Offen: PROJ-Y-114b (interner DMS-Verweis) · PROJ-Y-114c (ID-Hygiene). [Spec](PROJ-Y-114a-dd-finding-source-reference.md) |
| PROJ-Y-113c | **Resolved 2026-08-18 — eingeloest in PROJ-Y-114a** | **Nachtraeglich registriert.** Der Followup existierte bisher **nur** in der PROJ-113-Spec (4 Nennungen, u. a. als Reuse-Matrix-Zeile fuer AC4 „Frage → Finding eskalieren") und **nie in diesem Register** — ein zugesagter Followup, der im Register fehlt, ist praktisch verloren. PROJ-113 hatte die Spalte namentlich vorgezeichnet (`dd_findings.source_dd_question_id`, „downstream-Pattern") und an PROJ-114 uebergeben; PROJ-114 baute sie nie, hinterliess aber einen **deaktivierten** UI-Platzhalter. Beides ist mit PROJ-Y-114a erledigt: FK + Konsistenz-Waechter + Need-to-know-Tor und die aktive Eskalations-Aktion (Pentest-Vektoren D/G/H/L). |
| PROJ-Y-114b | Planned (Followup, 114) | **Interner DMS-Verweis als Quelle** eines DD-Findings. Heute nicht baubar bzw. sinnlos: `external_document_links.url` traegt per SSRF-Haertung CHECK `https://%` und kann keinen Baumknoten adressieren, und das DMS ist in Prod **leer** (`documents` 0, `document_tree_nodes` 0 — drei Wochen nach dem PROJ-79-α-Deploy). Gleiche Begruendung, mit der PROJ-80 seinen Vektor-Teil zurueckstellte: erst bauen, wenn Dokumente existieren. Dann eigene Verweis-Tabelle nach dem `skill_knowledge_links`-Muster (PROJ-77-γ) inkl. Vertraulichkeits-Tor aus PROJ-Y-115c — **nicht** `external_document_links` aufweichen. |
| PROJ-Y-114c | Planned (Followup, Hygiene) | **ID-Hygiene `PROJ-Y-1`.** Der Name ist fuenffach belegt; die globale Registerzeile ist mit PROJ-Y-114a aufgeloest, die vier **spec-lokalen** Vorkommen bleiben: PROJ-70 (OCR, Spec Z. 166), PROJ-98 (Stage-Gate `committee_stage_gates`, Z. 163/175), PROJ-100c (Pro-Objekt-„sensibel", Z. 157), PROJ-116 (Word-Export, Z. 19/37) — dazu PROJ-114s eigenes (E-Mail-/Teams-Eskalation, Z. 127/147). Auf eindeutige `PROJ-Y-<Eltern><Buchstabe>`-IDs umschreiben. Bewusst nicht in PROJ-Y-114a mit-erledigt: fremde Features, parallele Lanes. **Zusammengeführt 2026-08-21:** dieselbe Sache ist auf `main` unabhängig als **PROJ-Y-145h** registriert (Tranche 3 von PROJ-Y-145b hat die Kollision gemessen: `PROJ-Y-1` vier Bedeutungen, `PROJ-Y-2` vier, `PROJ-Y-3` drei, und für PROJ-98 bereits eindeutige IDs 98a–98d vergeben). Führende ID ist **PROJ-Y-145h**; diese Zeile bleibt als Verweis. |
| PROJ-Y-3 | Resolved 2026-08-04 (FE-only) | **Fixed.** Wizard step navigation no longer advances on a failed silent autosave. `goToStep` (src/components/projects/wizard/wizard-client.tsx:~406) now captures the `persistDraft` return and does `if (!saved) return` before `setStep(target)` — mirroring the abort-on-null guard already present in `onCreate` (finalize) and `onCancelSaveAndExit`. On a save error or an optimistic-lock conflict the user is held on the current step with unsaved changes; the error toast / conflict banner already surfaced (the error/warning toasts are NOT gated by `silent` — only the success toast is, so the original note's "error toast is suppressed too" did not reflect current code). Regression pinned by `src/components/projects/wizard/wizard-client.test.tsx` (3 cases: reject → hold, conflict → hold, success → advance; red-green verified against the pre-fix code). Gates: ESLint 0, tsc 12 baseline/0 new, vitest 2593/2593, build clean. No dep/migration. |
| PROJ-Y-145 | **Resolved 2026-08-12 — fortgeführt und geliefert als [PROJ-145](PROJ-145-portfolio-migration-deployment-scope.md)** | **Erledigt, ID zusammengeführt.** Die Spalte ist eingeführt, der Blocker weg: PROJ-130 (`mvp`) und PROJ-144 (`full`) sind gebucht, die 141 Altzeilen tragen den ausdrücklichen Marker `– nicht klassifiziert –`, und die Regel ist als `npm run check:index-scope` + Required-Check-Workflow ausführbar. **Warum umbenannt:** ein `PROJ-Y-`-Präfix bezeichnet in diesem Repo den Folgekandidaten eines Elternfeatures — ein Elternteil „145" gibt es nicht, und zwei IDs für eine Sache wären genau die Buchhaltungsfäule, gegen die die Slice antritt (D-145.1). **Zwei hier notierte Messwerte waren daneben und sind korrigiert:** „138 Deployed-Altzeilen" → **139** (auch **139** war noch zu niedrig; die belastbare Zahl ist **141**, gegen den Migrations-Commit `d1402bb` gemessen — siehe PROJ-Y-145b) (die Substring-Suche zählte PROJ-130 mit, dessen Status das Wort in einer Klammer trägt), und „fünf Zeilen mit Prosa-Pipes (PROJ-78/79/92/142/Y-142a)" → **vier** (PROJ-78/79/142/**144**; 92 und Y-142a tragen nur *escapte* Pipes, dafür trug 144 drei — eingebracht vom Messtext selbst). Offene Restarbeit als PROJ-Y-145a/b/c registriert. Ursprüngliche Analyse zum Nachlesen: **Portfolio-Migration: `Deployment Scope` als eigene Spalte in `features/INDEX.md` + evidenzbasierte Klassifikation des Altbestands.** `.claude/rules/general.md` verlangt Scope als **separates Feld** und der `/deploy`-Skill stoppt die Buchführung, solange die Spalte fehlt („never guess scopes for old rows"). Getroffen hat es bereits **zwei** Slices: PROJ-130 steht als `Approved (α–ε gebaut + live-verifiziert; Deployed/Scope pending Portfolio-Migration)` (INDEX Z. 155) und PROJ-144 als `Approved`, obwohl der Code live ist. **Bereits gemessen (2026-08-12), damit die Slice nicht neu erheben muss:** 164 PROJ-Zeilen, davon **104** schlicht `Deployed` + ~23 `Deployed (…)`-Varianten + 14 `Planned` + Einzelfälle; **alle 164** enden sauber auf `\| [Spec](…) \| YYYY-MM-DD \|`, der Umbau muss deshalb **am Zeilenende verankern statt an Feldnummern** — **fünf** Zeilen (PROJ-78/79/92/142/Y-142a) tragen Pipe-Zeichen in der Prosa und würden bei feldbasiertem Tausch zerreißen (sie rendern schon heute mit zu vielen Spalten: 8 bzw. 9 statt 7 Felder — eigener Bestandsfund, unabhängig von der Migration). Fertiges, aber noch nicht ausgeführtes Umbau-Skript samt Klassifikations-Regeln lag im Scratchpad der Session vom 2026-08-12 (nachbaubar in ~40 Zeilen). Nutzer-Entscheid zum Vorgehen: Spalte einführen, neue/verifizierte Slices sofort korrekt klassifizieren, Altzeilen **explizit** als „– nicht klassifiziert –" markieren und die Schuld sichtbar lassen statt zu raten; danach zeilenweise Audit gegen Spec/AC/QA/Code. Blockiert derzeit die `Deployed`-Buchung von PROJ-130 und PROJ-144. |
| PROJ-Y-145a | **Resolved 2026-08-13 — der Lauf existierte bereits, er war nur nicht mitgewandert** | **8/8 PASS gegen die deployte Vercel-Laufzeit** (Diktat → Entwurf 200 · Scrum→`story` · `requires_confirmation` · nutzer-private Liste · Bestätigung **201** · korrigierter Titel bis in die persistierte Zeile · `kind=story` · zweite Bestätigung **409 `draft_not_open`**), bewusst im Assistant-Testmandanten statt in Kundendaten; danach 0 Rückstände über fünf Tabellen, `audit_log_entries` unangetastet. Nachweis-Tabelle in der PROJ-144-Spec. **Warum der Eintrag überhaupt entstand — der eigentliche Befund:** der Lauf war am 2026-08-12 um **16:25** ausgeführt (`bb55ded`), die Scope-Spalte wurde um **17:04** aus einem rekonstruierten Branch gemergt, der nur den **früheren** Commit derselben Lane (`d918058`, 16:09) übernommen hatte — den Stand, in dem die Ausführung noch an einem Werkzeug-Ausfall gescheitert war. Also kein fehlender Nachweis, sondern ein beim Branch-Neubau verlorener. Lehre: wird ein Branch wegen fehlender CI-Läufe von `main` neu aufgebaut, gehören **alle** seine Commits geprüft, nicht nur der zuletzt gelesene — sonst wandert die Buchführung hinter den Nachweisen zurück. **An der Scope-Buchung ändert sich nichts:** `full` stand schon am Regeltext, dieser Lauf macht die Begründung nur unabhängig von jener Auslegung. |
| PROJ-Y-145b | **In Arbeit — Tranche 3 erledigt (117 → 82)** | **Zeilenweises, evidenzbasiertes Audit der Alt-`Deployed`-Zeilen** gegen Spec, Akzeptanzkriterien, QA-Ergebnis und Code; Klassifikation nach `full`/`mvp`/`alpha`/`tooling-only`, **kein** Rückschluss aus dem alten `Deployed`-Etikett. Spec zuerst, INDEX zuletzt. **Tranche 1 (2026-08-13, 141 → 128):** 13 Zeilen. Vier reine Propagation — PROJ-Y-143e/f/m (`full`) und PROJ-Y-143h (`tooling-only`) trugen den Scope **längst im Spec-Header**, die Migration hatte sie pauschal als Legacy markiert (Befund über PROJ-145 selbst). Neun durch Lesen belegt: PROJ-42 + PROJ-134 → `alpha`; PROJ-67, PROJ-143, PROJ-Y-142a, PROJ-Y-142c, PROJ-Y-143d, PROJ-Y-143g → `tooling-only`; PROJ-74 → siehe Korrektur unten. **Tranche 2 (2026-08-13, 128 → 120):** 8 Zeilen. `full`: **PROJ-68** (DB-Hygiene, AC-1/2/3/4/7 ✅, AC-5/6 inherently green per Prod-Smoke), **PROJ-136** (7/7 AC QA-PASS und zusätzlich ein HIGH-Bugfix inkl. Migration in PROJ-70 ausgeliefert), **PROJ-140** + **PROJ-142** (Remediation von **Produktions**-Abhängigkeiten, `audit` exit 0), **PROJ-Y-142b** (genau eine Produktivzeile — der Full-Buffer-Sniff-Fix — also echte Laufzeitänderung), **PROJ-Y-115c** (Sicherheits-Tor: 2 Migrationen, 12 RESTRICTIVE-Policies, Pentest 17/17 + 5/5), **PROJ-Y-122a** (Reconcile-Migration in Prod, Live-Smoke grün). `tooling-only`: **PROJ-138** (Verdikt Approved, reines E2E-Setup). **Grenzregel für `tooling-only`, in Tranche 2 präzisiert und angewandt:** `tooling-only` heißt, die Änderung lebt in Repo-Tooling, CI, Tests oder Doku und verändert **nicht, was in Produktion läuft**. Alles was Produktions-Laufzeit anfasst (in Prod ausgelieferte Abhängigkeiten, DB-Objekte, `src/`-Verhalten) ist nicht `tooling-only`; wenn vollständig, dann `full`. **Korrektur aus Tranche 1: PROJ-74 `tooling-only` → `full`.** Die Slice hat `src/lib/sentry-config.ts` ausgeliefert, also Produktionscode, der verändert was die Anwendung nach außen sendet (AC 5/6: `beforeSend`-Scrubber über Server-, Edge- und Client-Entrypoint). Unter der oben präzisierten Grenzregel war `tooling-only` falsch; die sieben ACs sind vollständig erfüllt, daher `full`. **Messung aus Tranche 1 korrigiert.** Der dort veröffentlichte Split (9 / 102 / 13 / 17) stammte aus einem Parser, der den AC-Abschnitt an der **ersten** folgenden Überschrift beendete — Specs mit Unterblöcken (`### Block A`) wurden dadurch als „AC-Abschnitt ohne Items" gezählt, obwohl sie Kriterien haben. Korrekt über die 128 zum Tranche-2-Start offenen Zeilen: **9** ohne AC-Abschnitt · **33** mit Abschnitt aber ohne Checklisten-Items (ACs als Prosa/Tabelle) · **12** vollständig angehakt · **8** gemischt · **66** mit Items, von denen **kein einziges** angehakt ist (PROJ-32 0/59, PROJ-62 0/42, PROJ-1 0/28). Die Schlussfolgerung aus Tranche 1 wird davon nicht schwächer, sondern stärker: **Checkbox-Zustand ist als Beleg unbrauchbar**, der Audit liest Statusheader, QA-Verdikt, Deviations und diese Datei. **Zwei Zeilen bewusst nicht klassifiziert** (Regellücke, siehe PROJ-Y-145f): **PROJ-69** — `AC-6` („≤ 30 INFO") ist laut Spec strukturell unerreichbar, geliefert sind 0 WARN und 166 dokumentierte INFO; nichts wurde zurückgestellt, das Kriterium ist aber wortwörtlich unerfüllt. **PROJ-29** — der Block-C-Smoke *überspringt* (fehlender Service-Role-Key), das Kriterium „ein Test zeigt die App-Shell nach Login" ist damit nicht demonstriert, ebenfalls ohne Followup-Registrierung. Für beide passt weder `full` (AC unerfüllt) noch `mvp`/`alpha` (nichts offen, kein benannter Followup). Nicht geraten. **Nachgeholt 2026-08-14 (119 → 117, gemessen statt abgeleitet):** die Regellücke ist mit PROJ-Y-145f entschieden (Waiver-Regel), und die zwei Zeilen sind gebucht — **beide `full`**. Dabei hat sich die Tranche-2-Begründung für **PROJ-29 als überholt erwiesen**: der Block-C-Smoke überspringt nicht mehr (2026-08-14 gemessen: 1 passed, 21,1 s), und die QA-Matrix vom 2026-05-01 führte die AC ohnehin als ✅ — PROJ-29 war also nie ein Waiver-Fall für *dieses* Kriterium, wohl aber für zwei andere (QA-Findings M1/M2, siehe PROJ-Y-145f-w2). **PROJ-69/AC-6** ist der eine echte Fall (PROJ-Y-145f-w1). Lehre für die Tranchen 3–5: eine Zeile, die beim Audit „nicht klassifizierbar" wirkt, kann schlicht **veraltet gelesen** sein — vor dem Zurückstellen prüfen, ob ein späterer Slice die Lücke längst geschlossen hat. **Offen: 117 Zeilen** (`npm run check:index-scope`, 2026-08-14 — die hier zuvor notierten „120" waren beim Schreiben schon veraltet; parallele Slices klassifizieren ihre eigenen Zeilen mit, deshalb ist die Restzahl **immer zu messen, nie fortzuschreiben**). Nächste Tranchen: **(3)** M&A-Familie PROJ-94–132 (dicht dokumentiert, viele mit ausdrücklichen Deferrals — hier ist `alpha`/`mvp` häufig zu erwarten). **(4)** Kern PROJ-1–50 (älteste, dünnste Belege, echte Archäologie). **(5)** Rest der Y-Familien. Fortschritt bleibt bei jedem Lauf messbar: `npm run check:index-scope` nennt die Restzahl. **Tranche 3 (2026-08-20, 117 → 82): 35 Zeilen, die ganze M&A-Familie PROJ-94–132 plus die zwei M&A-Y-Zeilen (Y-96b, Y-96e).** Ergebnis **13 `full` · 21 `mvp` · 1 `alpha`** — die Vorhersage aus Tranche 2 („hier ist `alpha`/`mvp` häufig zu erwarten") trifft zu, und der Grund ist strukturell: die Familie wurde als **verzahnte Kette** gebaut, in der jede Slice Teile an ihre Nachfolger abgegeben hat. Aufstellung je Zeile mit Quell-AC und Ziel-ID im eigenen Abschnitt „Tranche 3" dieser Datei; die Begründung steht zusätzlich als Zitat unter dem `## Deployment Scope`-Header jeder Spec. **Die Tranche-2-Lehre („eine Zeile kann veraltet gelesen sein") hat sich mehrfach bestätigt, aber nie zu einem Upgrade geführt:** PROJ-95 (Deliverable-Link → PROJ-104), PROJ-102 (Reporting/Templates → 131/132/96), PROJ-104 (Templates → 96), PROJ-112 (Zähler → 113/114), PROJ-116 (Pflicht-Deliverables → mit #292 geliefert) sind sachlich geschlossen, bleiben aber `mvp`, weil die Hausregel für `alpha → mvp → full` einen **eigenen** QA-/Deploy-Pass verlangt — als „Upgrade-Kandidat" markiert statt stillschweigend hochgebucht. **Zwei Unterscheidungen trugen die Klassifikation, beide am AC-**Wortlaut** entschieden, nicht am Verdikt:** (1) ein AC, das seinen **Ort** nennt, ist am falschen Ort nicht erfüllt — PROJ-103 AC3 verlangt Top-3 „auf dem Projekt-Dashboard", geliefert ist die Engpässe-Seite (`mvp`), während PROJ-131 AC-131-5 ausdrücklich **Platzhalter** verlangte und die auch bekam (`full`); (2) ein AC, das sich selbst als **„optional"** bezeichnet, blockiert `full` nicht — PROJ-117 AC5 („optional … offene Frage", ICS statt Sync) und PROJ-115 AC3 („Optional … VDR") sind deshalb **keine** Auslassungen, wohl aber PROJ-115 AC2, dessen Erreichbarkeitsprüfung nicht optional formuliert ist. **Drei Korrekturen an Fremdbefunden:** PROJ-100c — das QA-Verdikt sagt „11/11 AC", die Spec führt **12** numerierte Kriterien (1, 2, 3, 4, 5, 5b, 6, 7, 8, 9, 10, 11) und AC-100c-8 ist deferiert; PROJ-99 und PROJ-128 tragen je ein AC, das die Spec selbst als „Offen" führt, **ohne** dass je eine Followup-ID vergeben wurde (jetzt PROJ-Y-99a, PROJ-Y-128a); PROJ-129 hat AC2 bewusst von vier auf drei Stufen reduziert und AC5 gar nicht gebaut (PROJ-Y-129a). **Ein Befund über die Tranche hinaus:** die Kennungen `PROJ-Y-1`, `PROJ-Y-2`, `PROJ-Y-3` sind **mehrfach belegt** — `PROJ-Y-1` bezeichnet vier verschiedene Followups, `PROJ-Y-2` vier (darunter eines, das das Register als **erledigt** führt, während drei gleichnamige offen sind). Für PROJ-98 sind eindeutige IDs vergeben (98a–98d), die Umbenennung in 114/116 ist **PROJ-Y-145h**. **Offen: 82 Zeilen** (`npm run check:index-scope`, 2026-08-20 — Restzahl bleibt zu **messen**, nicht fortzuschreiben). Nächste Tranchen: **(4)** Kern PROJ-1–50 (älteste, dünnste Belege), **(5)** Rest der Y-Familien. |
| PROJ-Y-145c | **Erledigt 2026-08-13** | Der Context `Verify lifecycle status vs deployment scope in features/INDEX.md` ist im Ruleset **`main protection`** (ID 15992143) als **Required Check** eingetragen; verifiziert durch erneutes Lesen von der API, nicht aus der eigenen Schreib-Antwort. Vorher 3 Checks, jetzt 4. **Nachtrag 2026-08-13 (Korrektur einer eigenen Ungenauigkeit):** auf `refs/heads/main` stehen **zwei** aktive Rulesets — neben `main protection` auch `main protection1` (id 15994143), das `Vercel Preview Comments` (integration 8329) verlangt. Es blockiert genauso; zusammen sind es **sechs** Contexts, nicht fünf. Die Zahl war in dieser Zeile und in CLAUDE.md zu niedrig, weil nur das erste Ruleset betrachtet wurde — eine dritte Session lief deshalb in eine PR, die bei fünf grünen Actions-Checks `BLOCKED` blieb. CLAUDE.md nennt jetzt beide Rulesets und trennt den erzwungenen Context `Vercel Preview Comments` vom nicht eingetragenen Build-Check `Vercel`. Vorgehen: vollständiges Ruleset lesen → nur die Check-Liste ergänzen → zurückschreiben (`bypass_actors` war und bleibt leer, `conditions` unverändert `refs/heads/main`) — ein Feld-für-Feld neu getipptes Ruleset hätte die übrigen Regeln (`deletion`, `non_fast_forward`, `pull_request`) verloren. Gefahrlos, weil der Workflow auf `pull_request: [main]` triggert und auf PR #361 grün lief, also keine PR hängen lässt. **Bestandsfund dabei:** `Verify migration filename naming + version-prefix uniqueness` steht seit PROJ-134 in der CLAUDE.md-Tabelle „A PR cannot merge until all of these pass", ist aber **nie eingetragen** worden — dieselbe Unwahrheit, die für den neuen Check vermieden werden sollte. Nicht stillschweigend mit-eingetragen (fremdes Feature, Branch-Protection-Änderung = Eigentümer-Entscheidung), sondern in der Tabelle ehrlich als „runs on every PR, not enrolled" annotiert → **PROJ-Y-145d**. Zusätzlich hält die Tabellen-Einleitung jetzt fest, dass Enrollment im Ruleset lebt und nicht in der Datei. **Nachtrag 2026-08-13:** dieser Absatz beschreibt den *Befund von damals* — der Nutzer hat daraufhin entschieden, und **PROJ-Y-145d ist erledigt**: der Migration-Naming-Check ist seit dem 2026-08-13 ebenfalls eingetragen (5 Required Checks), die Annotation in CLAUDE.md ist entfernt und die Tabelle trägt keine Vorbehalte mehr. |
| PROJ-Y-145d | **Erledigt 2026-08-13** | Der Context `Verify migration filename naming + version-prefix uniqueness` ist im Ruleset **`main protection`** (ID 15992143) als **Required Check** eingetragen — vorher 4 Checks, jetzt **5**. Damit ist PROJ-134s F-1-Handoff eingelöst, der seit dem 2026-07-01 offen stand: die CLAUDE.md-Tabelle führte den Check die ganze Zeit unter „A PR cannot merge until all of these pass", tatsächlich war er nur advisory. **Nutzer-Entscheid** (der Eintrag war bewusst nicht mit-erledigt worden, weil eine Branch-Protection-Änderung dem Repo-Eigner gehört). **Vorgehen wie bei 145c:** vollständiges Ruleset lesen → **nur** die Check-Liste ergänzen → zurückschreiben → **erneut von der API lesen** und gegen den Vorher-Snapshot diffen, statt der eigenen Schreib-Antwort zu glauben. Ergebnis des Diffs: einzige Änderung ist der eine hinzugefügte Context; die vier bestehenden stehen unverändert und in derselben Reihenfolge, `deletion`/`non_fast_forward`/`pull_request` sind byte-identisch, `bypass_actors` weiterhin leer, `conditions` weiterhin `refs/heads/main`; `main protection1` (15994143) unberührt. **Name doppelt belegt, bevor er zur Sperre wurde:** Job-`name:` in `migration-naming.yml` und `check-runs` auf `main` melden beide exakt denselben String — ein Tippfehler hätte jede künftige PR unmergebar gemacht. **Kein Pfad-Filter** (`on: pull_request: branches: [main]`), der Check läuft also auf jedem PR und kann nicht als „Expected — Waiting for status" hängen bleiben. **Rauchprobe am eigenen PR #370:** der neue Context erschien dort als *required* und lief grün — die Buchung dieser Zeile ist zugleich ihr eigener Nachweis. **Nebenbefund, nicht von dieser Einschreibung verursacht:** die beiden offenen PRs #368/#369 tragen **gar keine** GitHub-Actions-Läufe, weil sie `mergeStateStatus: DIRTY` sind — bei Merge-Konflikten baut GitHub die Merge-Ref nicht, auf der `pull_request`-Workflows laufen. Sie waren damit schon vorher durch die **vier** bestehenden Required Checks blockiert; der fünfte ändert daran nichts. Auflösung liegt bei ihren Sessions (Konflikt auflösen → Läufe starten). |
| PROJ-Y-145e | Planned (Followup, 145) | **Statusheader-Typografie normalisieren.** Drei Specs führen den Status als fettes `**Status:**` statt als `## Status:`-Überschrift; PROJ-143 mischt seit Tranche 1 von PROJ-Y-145b beides (fetter Status + `## Deployment Scope:`). Bewusste Abweichung: der Scope wurde in der regelkonformen, greppbaren `## `-Form gesetzt (`.claude/rules/general.md` verlangt separate Header), statt die Fremd-Typografie zu übernehmen — eine Spec umzuformatieren gehört nicht in eine Klassifikations-Tranche. Aufräumen, sobald die Datei aus anderem Grund angefasst wird. Kein Funktionsrisiko: der `check:index-scope`-Wächter liest die INDEX, nicht die Spec-Header. |
| PROJ-Y-145f | **Erledigt 2026-08-14 — Option (c) als Regel aufgenommen** | **Nutzer-Entscheid:** die Regel bekommt eine eng gefasste Ausnahme statt eines sechsten Scope-Werts. In `.claude/rules/general.md` steht jetzt der Abschnitt **„Waived criterion"**: `full` bleibt zulässig, wenn ein Kriterium wörtlich unerfüllt ist, aber **alle vier** Bedingungen gelten — (1) nichts zurückgestellt, (2) nachweislich unerreichbar oder überholt und **heute re-verifizierbar**, (3) eine schriftliche Abnahme benennt das Kriterium, (4) die Substanz ist durch andere Messung belegt. Der Waiver wird zweifach protokolliert: neben dem Kriterium in der Spec und als **geschlossener** Eintrag hier. Ausdrücklich als enge Ausnahme markiert, nicht als Ermessensspielraum: „QA hat es abgenommen" erfüllt allein Bedingung 3. Pointer in `CLAUDE.md` gespiegelt, damit die beiden Dateien nicht driften. **Verworfen:** (a) sechster Wert (Taxonomie-Inflation plus Guard-Umbau für einen seltenen Fall), (b) `mvp` (behauptet verfolgte Restarbeit, die es nicht gibt, und erzwingt ein Followup, das nie jemand erledigt — unehrlich in die Gegenrichtung). **Befund, der den Anlass halbiert hat:** von den zwei Auslöser-Zeilen ist **PROJ-29 gar kein Waiver-Fall** — die Tranche-2-Begründung („der Block-C-Smoke überspringt") ist überholt, am 2026-08-14 gemessen läuft `tests/PROJ-29-auth-fixture-smoke.spec.ts` grün (1 passed, 21,1 s, kein Skip), und in der QA-Matrix von 2026-05-01 stand die AC ohnehin schon ✅. Übrig blieb **PROJ-69/AC-6**. Beide Zeilen sind jetzt klassifiziert (siehe die zwei folgenden geschlossenen Einträge). |
| PROJ-Y-145f-w1 | **Geschlossen — kein offener Rest** (Waiver, PROJ-69/AC-6) | **Abgeschriebenes Kriterium: „`get_advisors(performance)` zeigt 0 WARN und ≤ 30 INFO".** Geliefert 0 WARN und 166 INFO. Vier Bedingungen live geprüft 2026-08-14: (1) kein Followup nötig — die 84 bewussten FK-Skips *sind* der gewollte Endzustand; (2) unerreichbar, heute deutlicher als damals — der Advisor kann dokumentierte Entscheidungen nicht unterdrücken, die Zahl **wächst** mit dem Schema: aktuell **289 INFO**; (3) die Deviation steht seit 2026-06-11 wörtlich in der AC-Zeile; (4) Substanz gemessen — **65 Index- + 91 Constraint-Kommentare** in Prod tragen weiterhin ihre Begründung am Objekt. Nichts offen, daher **geschlossen** statt Followup. |
| PROJ-Y-145f-w2 | **Geschlossen — kein offener Rest** (Waiver, PROJ-29/Block A + Block C) | **Zwei abgeschriebene Kriterien.** (a) „Ausnahme zulässig nur wenn der Lint-Hit auf 3rd-Party-Code zeigt" — 5 file-pattern Overrides, davon 3 für React-19-Regeln, die auf legitime Muster fehlfeuern (QA-Finding M1). (b) „Test-User-Seed als idempotente SQL-Migration" — real per `globalSetup`-Admin-Upserts, weil `auth.users` per SQL-INSERT brüchig ist (QA-Finding M2). Für beide: nichts zurückgestellt · schriftliche Abnahme in der QA-Matrix vom 2026-05-01 (🟡 „Documented deviation", Verdikt Approved, 0 Critical/High) · Substanz am 2026-08-14 nachgemessen — `npm run lint` exit 0, und der `globalSetup`-Seed lief im Messlauf sichtbar durch. Nichts offen, daher **geschlossen**. |
| PROJ-Y-143l-w1 | **Geschlossen — kein offener Rest** (Waiver, PROJ-Y-143l/AC-Y143l.9) | **Abgeschriebenes Kriterium: „Null Rückstände in der Produktionsdatenbank aus den Experimenten".** Wörtlich unerfüllt: **6 Zeilen** in `audit_log_entries` bleiben stehen (`__created`/`__deleted` je für `tenants`, `tenant_memberships`, `tenant_settings` des Probe-Mandanten aus den Isolationsversuchen A/B1/B2). Vier Bedingungen geprüft: (1) kein Followup nötig **und keines möglich** — PROJ-Y-130h/143o betreffen *künftiges* Test-Rauschen, und 143o stellt selbst fest, dass bereits entstandene Zeilen „nicht rückholbar" sind; (2) strukturell unerreichbar — seit **PROJ-130-α** ist der Trail append-only, die Wächter `audit_log_no_update`/`_no_delete`/`_no_truncate` liefern `42501` für **jede** Rolle inklusive `service_role` und `postgres`, und der Mandanten-FK wurde in derselben Migration entkoppelt, damit Zeilen das Löschen ihres Mandanten überleben; das ist gewollte Produkteigenschaft, kein Defekt, re-verifizierbar über den PROJ-130-α-Pentest (19/19); (3) schriftliche Abnahme benennt es — `D-Y143l.4` plus der Abschnitt *Rückstandsprüfung*, der die Ausnahme einzeln auflistet (**kein** QA-Verdikt behauptet: für 143l ist kein `/qa` gefahren, die Abnahme ist der Deviation-Eintrag); (4) Substanz gemessen — Probe-Mandant 0 per ID **und** per Name, dessen Mitgliedschaften/Settings/Projekte 0/0/0, geteilter Nutzer zurück auf 2 Mitgliedschaften, beide Modulsätze auf Ausgangswert, `tenant_memberships_admin_invariant_delete` wieder scharf. Die sechs Zeilen sind ein Protokoll **über** das Experiment, kein vom Experiment hinterlassener Zustand; sie sind symmetrisch und wurden bewusst nicht per Trigger-Umgehung unterdrückt. **Anmerkung zur Regelanwendung:** der Abschnitt „Waived criterion" ist für **`full`** geschrieben, 143l ist `tooling-only` (Aussage über die Lieferachse, nicht über AC-Vollständigkeit) — die vier Bedingungen sind hier also nicht formal erzwungen, sondern als strengster verfügbarer Maßstab angelegt, weil Schritt 4 des Buchführungs-Verfahrens jede akzeptierte Auslassung scope-unabhängig sichtbar verlangt. Nichts offen, daher **geschlossen** statt Followup. |
| PROJ-Y-143n | **Erledigt 2026-08-19 — Variante A umgesetzt und deployed** (Tag `v2.62.0-PROJ-Y-143n`) | **Korrigiert 2026-08-18 (Requirements-Lauf).** Hier stand „Der `organization`-Modulschalter ist wirkungslos" — **gemessen ist er halb wirksam**, und das ist der schlechtere Zustand: `src/app/api/organization-imports/_helpers.ts:48-53` gatet **alle fünf** CSV-Import-Routen aus PROJ-63 (einzige Produktiv-Aufrufstelle; UI-seitig null), die **12 Handler in 8 Dateien** der Kernfläche dagegen nicht. Live in **4 von 6** Mandanten: `/stammdaten/organisation` funktioniert, der Import rendert einen roten Fehlerkasten (zusätzlich ein PROJ-Y-143f-Defekt, `use-organization-imports.ts:66-68` ohne `unavailable`-Feld). **Wiederentdeckung von Bug M2-Re aus PROJ-62** (QA 2026-05, Folge-Slice nie gebaut), obwohl PROJ-62 AC Zeile 97 das Tor wörtlich für „alle UI- und API-Pfade" verlangt. Folgenabschätzung gemessen: **0 Entfernungen** von `organization` im Audit-Trail bei 23 `active_modules`-Änderungen, **0** belegte `organization_unit_id`-Verweise, **0** Fremdkonsumenten, nur der Produktivmandant hält Daten (3 Einheiten + 1 Standort) und hat den Schalter an → Variante A trifft heute **0** Mandanten. Spec mit 16 AC und offenem Fork: [PROJ-Y-143n](PROJ-Y-143n-organization-module-switch.md), Empfehlung **A** (Tor nachrüsten), tragender Grund = die Zusage steht in der ausgelieferten Migration `20260509220000_proj62_…:478-500`, nicht in einer umsortierbaren Typ-Liste. Kein Sicherheitsbefund (RLS + Member/Admin-Gates greifen unabhängig). **Nicht implementiert**, weil A das API-Verhalten für Bestandsmandanten ändert und B eine dokumentierte Fähigkeit zurücknimmt. **Erledigt 2026-08-19 (Variante A).** Alle 12 Handler rufen das Tor (5 lesend → 404, 7 schreibend → 403); die zwei Sonderfälle gelöst: `[id]`-Handler behalten den Mandanten aus der geladenen Zeile (ein Tor auf dem *aktiven* Mandanten hätte die Einstellungen des falschen Mandanten befragen können — vier Tests lassen beide auseinanderfallen und prüfen, welchen das Tor liest), `move` bekommt denselben Anker wie die Geschwister und erreicht die RPC bei geschlossenem Tor nicht. UI-Hälfte nach PROJ-Y-143f in fünf Hooks und auf beiden Seiten, womit der rote Fehlerkasten der Import-Seite weg ist; Stammdaten-Kachel trägt jetzt `requiresModule`. Live gegen Prod in **beiden** Schalterstellungen (Testmandant, exakt zurückgeschrieben), RLS-Nicht-Regress **6/6 PASS / 0 Rückstände**, Produktivmandant unverändert (3 Einheiten, 1 Standort, Modul an). Rot-Grün: ohne Gates fallen 25 von 53 Route-Fällen. Gates ESLint 0 · tsc 0 neu · vitest 3109/3109 · Playwright 17/17 · Visual 3× 9/9 (nur `stammdaten.png`, im Bild geprüft). Buchführung von PROJ-Y-143k, PROJ-62 (M2-Re + AC 97 + 403-Formulierung als überholt gekennzeichnet) und PROJ-63 nachgezogen. **Offen bleibt bewusst:** die 11 übrigen Modulschlüssel sind erhoben, nicht saniert — und „≥ 1 Aufrufstelle" ist ausdrücklich **keine** Abdeckungsaussage, denn genau diese Metrik hat den halben Schalter drei Monate unauffällig gemacht. **Geschlossen 2026-08-19:** Nutzer entschied **Variante A**; das Tor sitzt jetzt an allen 12 Handlern (5 lesend → 404, 7 schreibend → 403), der halbe Zustand ist beseitigt, die Kachel trägt `requiresModule` und der rote Fehlerkasten am Import ist weg. PR #406 → main `0280610`, Prod-Deployment 5978088984 `success`; Live-RLS-Pentest 6/6 gegen Prod ohne Rückstände, Post-Deploy-Lesevektor bestätigt den Produktivmandanten unverändert (3 Einheiten, 1 Standort, Modul an). Scope `full`. **Kein Rest** aus dieser Zeile: die übrigen 11 `ModuleKey`s waren per AC-Y143n.1 nur zu **erheben**, nicht zu sanieren — erhoben und protokolliert. Offen bleibt allein der in PROJ-Y-143k benannte Sonderfall, dass ein Schlüssel ohne ausgelieferte Fläche existiert; das ist eine eigene Frage und keine Auslassung hier. |
| PROJ-Y-145g | Planned (Beobachtung, aus 145f-Messung) | **Advisor- und Lint-Drift seit PROJ-68/PROJ-29 — beim Messen aufgefallen, nicht gesucht.** Drei Zahlen, alle am 2026-08-14 erhoben: (1) `get_advisors(performance)` meldet **15 WARN** (14 × `multiple_permissive_policies`, 1 × `unindexed_foreign_keys`) — PROJ-68 hatte die WARN-Zahl auf **0** gebracht und PROJ-69 sie dort gelassen; die 15 stammen aus späteren Slices, vor allem der M&A-Familie mit ihren RESTRICTIVE-/permissiven Policy-Paaren. (2) Von heute **195** unindexierten FKs sind rund **100** nach PROJ-69s Triage entstanden und tragen **keinen** dokumentierenden Kommentar — die Zusicherung „jedes INFO ist eine dokumentierte Entscheidung" gilt nur noch im Umfang von PROJ-69. (3) **47** Dateien tragen per-line `eslint-disable-next-line`; PROJ-29 schloss mit null, PROJ-67 notierte 23→26. Die Lint-Baseline selbst ist grün (`npm run lint` exit 0). **Keine dieser Zahlen ist ein Mangel von PROJ-29 oder PROJ-69** — beide können für Code nicht haften, den es zu ihrer Zeit nicht gab; deshalb eigener Eintrag statt Rückbuchung. Wert einer Slice: die Policy-WARNs sind echte Mehrfach-Policy-Auswertung pro Query (PROJ-68 war genau dafür da), die FK-Lücke ist Triage-Rückstand. Vor einer Slice die Triage-Regeln aus `docs/db-index-audit-2026-05-30.md` erneut anwenden statt neu erfinden. |
| PROJ-Y-145f-orig | _Ursprüngliche Fassung, zur Nachvollziehbarkeit_ | **Regellücke in der Scope-Taxonomie: ein Kriterium ist unerfüllt, aber nichts ist offen.** Aufgetreten in Tranche 2 von PROJ-Y-145b an zwei Zeilen. **PROJ-69:** `AC-6` verlangt „0 WARN und ≤ 30 INFO"; geliefert sind 0 WARN und 166 INFO, und die Spec begründet, dass die ≤-30-Grenze mit 84 bewussten FK-Skip-Entscheidungen **strukturell unerreichbar** ist (der Advisor kann dokumentierte Skips nicht unterdrücken). **PROJ-29:** der Block-C-Auth-Fixture-Smoke überspringt aus Umgebungsgründen, das Kriterium „ein Test zeigt die App-Shell nach Login" ist also nicht demonstriert; die Spec nennt die Behebung eine „optionale" Folgearbeit (faktisch später durch PROJ-143 / PROJ-Y-78f eingelöst). Keiner der vier Werte passt: `full` verlangt jedes in-scope-Kriterium erfüllt · `mvp` und `alpha` verlangen benannte, verfolgte Restarbeit, die es hier nicht gibt · `tooling-only` ist eine Aussage über die Lieferachse, nicht über Vollständigkeit. Die Regel verbietet zudem ausdrücklich, Original-Kriterien nachträglich umzuschreiben, nur um `full` zu rechtfertigen — genau das wäre der bequeme Ausweg. **Zu entscheiden:** (a) fünfter Wert bzw. Zusatz-Markierung für „vollständig geliefert, ein Kriterium als unerreichbar abgeschrieben"; (b) solche Fälle als `mvp` buchen und die abgeschriebene Anforderung hier als geschlossenes (nicht offenes) Followup benennen; (c) `full` erlauben, wenn QA die Abweichung ausdrücklich abgenommen hat, und die Abnahme als Nachweis verlangen. Betrifft absehbar weitere Zeilen der Tranchen 3–5. |
| PROJ-Y-146a | **Resolved 2026-08-13 — 13/13 gegen die deployte Funktion** | `POST /api/projects/{id}/snapshots` gegen `https://projektplattform-v3.vercel.app` → **HTTP 200**, `pdf_status='available'`, PDF **33.190 Bytes** mit `%PDF-`-Magic im `reports`-Bucket, in der Datenbank gegengeprüft; zweimal unabhängig gefahren (11,9 s / 13,3 s). **An `puppeteer-core@25` gebunden statt bloß „hat funktioniert“:** Produktions-Deployment `542f412` (`state=success`, 10:23:32Z) enthält den Bump `1cbdc4b` und pinnt `^25.6.0`; die Probe lief um 10:26/10:30Z, also danach. **Nicht leerlaufend:** die Route setzt bei Render-Fehler `pdf_status='failed'`, `'available'` ist nur über einen echten Chromium-Start + HTTP-Abruf der Druckseite erreichbar. **Kundendaten unberührt:** eigener Wegwerf-Mandant mit aktivem `output_rendering` statt Modul-Umschaltung auf einem geteilten E2E-Mandanten (PROJ-Y-144d-Begründung); Gegenprobe nach dem Aufräumen `report_snapshots` über alle Mandanten = 10 = Bestand des Produktivmandanten, geteilte E2E-Identität wieder bei 2 Mitgliedschaften. Reproduzierbar als `scripts/verify-prod-snapshot-render.mts` (hinter `PROD_WRITE_ACK=1`, kein npm-Alias). Zwei Nebenbefunde → PROJ-Y-146b. |
| PROJ-147 | **Geschlossen 2026-08-20 — Deployed / Scope `tooling-only`, keine offene Auslassung** | **Der Ruleset-Tausch ist ausgefuehrt.** `Snyk production dependency scan` aus `main protection` (15992143) **ausgetragen**, `OSV scan of the dependency lockfile` **eingetragen**, in **einem** PUT. Unabhaengig nachgelesen: fuenf Contexts, alle vier Rules erhalten, `enforcement=active`, `strict=true`, `main protection1` unberuehrt → weiterhin sechs blockierende Gates. Danach — und nur danach — der Snyk-Job entfernt. **Drei Korrekturen an dieser Zeile selbst**, weil sie in drei Punkten veraltet war: (1) sie nannte die Kriterien `AC-147.6`/`AC-147.7`, die Spec fuehrt sie als **AC-147.7/AC-147.8** (Nummern-Drift); (2) sie nannte als Ersatz `Dependency review of new dependencies` — dieses Werkzeug ist per **D-147.2** verworfen, weil es in diesem Repo gemessen abbricht (Dependency Graph aus), eingetragen wurde der OSV-Scanner-CLI-Check; (3) die Begruendung „vom Klassifikator abgewiesen" galt **sitzungsabhaengig**, nicht aktionsbezogen — derselbe PUT ging spaeter durch. Als Handoff zu notieren war richtig, als dauerhaft unmoeglich zu lesen waere falsch gewesen. |
| PROJ-Y-146b | **Resolved 2026-08-13 — als Runbook dokumentiert, zwei Behauptungen dabei korrigiert** | **Ergebnis:** [`docs/production/prod-test-fixtures.md`](../docs/production/prod-test-fixtures.md) + Kurzregel in `CLAUDE.md` neben der Zero-Residue-Regel. Sechs Regeln, alle Zahlen und Fehlercodes **live gemessen**, nicht erschlossen: (1) nie der Kundenmandant, mit Gegenprobe über den Gesamtbestand; (2) für reine Erkundung eine **zurückgerollte** Transaktion — die Audit-Trigger schreiben *innerhalb* der Transaktion, ein Rollback kostet also **0** Zeilen (verifiziert: Probe mit Mandant, Mitgliedschaft und drei provozierten Constraint-Verletzungen → `audit_log_entries = 0`); (3) wenn committet werden muss, eigener Wegwerf-Mandant statt Modul-Umschaltung auf einem geteilten `[E2E]`-Mandanten (PROJ-Y-144d-Begründung, PROJ-Y-143f-Schadensfall); (4) `audit_lifecycle_exempt` **vor** dem Seeden setzen; (5) Teardown-Falle; (6) Rückstände zählen statt annehmen. **Zwei Korrekturen an dem, was ich selbst notiert hatte:** die Demotion des letzten Admins ist **auch** blockiert (ich hatte sie als möglichen Ausweg vermutet) — alle drei Wege scheitern mit `sqlstate 23514 / "Tenant must have at least one admin"`, nachgewiesen in einer zurückgerollten Transaktion mit echtem Fehlertext; und die Aussage „nur test-markierte Mandanten dürfen die Ausnahme tragen" gilt **nicht** laufend — siehe PROJ-Y-146c. **Bewusst nicht behoben:** weder die Invariante angefasst (Kerntabelle, hoher Blast-Radius, und Offboarding läuft ohnehin über PROJ-17 statt Hard-Delete) noch die Ausnahme automatisiert — ein Flag, das sich aus dem Namenspräfix ableitet, wäre genau die stille Lücke, gegen die PROJ-130 antritt. Beides als Entscheidung offengelegt statt nebenbei geändert. |
| PROJ-Y-146c | **Resolved 2026-08-13 — Schreibwächter in Prod, Live-Pentest 10/10, 0 Rückstände** | **Ergebnis:** Migration `20260813170000_projy146c_audit_exempt_write_guard` in Prod — Trigger-Paar `tenants_audit_exempt_write_guard` (BEFORE UPDATE, `WHEN old.audit_lifecycle_exempt is distinct from new…`) und `tenants_audit_exempt_insert_guard` (BEFORE INSERT, `WHEN new.audit_lifecycle_exempt`) auf `public.enforce_audit_exempt_write()`. **Die Frage wurde umgestellt:** nicht „*welche* Mandanten dürfen ausgenommen sein" (Option 3/4 — namens- bzw. tabellen-gebunden und beide selbst umgehbar), sondern „*wer* darf es entscheiden". Umgesetzt ist damit Option (1) rollen-gebunden und **fail-closed**: erlaubt sind `service_role`, `postgres`, `supabase_admin`; jede andere Rolle bekommt `42501`. **Gemessene Grundlage** (zurückgerollte Prod-Probe): `current_user` trennt die vier Wege eindeutig — direkt `postgres` · PostgREST `authenticated` · Service-Role `service_role` · `anon`; `session_user` ist auf allen vier `postgres` und untauglich, `auth.role()` ist auf dem direkten Weg **null** und hängt an JWT-Claims. `authenticated` ist in **keiner** Rollenmitgliedschaft von `service_role`/`postgres` → kein `SET ROLE`-Aufstieg. **Die Falle, an der der Wächter eine Attrappe geworden wäre:** unter `SECURITY DEFINER` ist `current_user` der Funktionseigentümer, die Prüfung ließe **jeden** durch — die Funktion ist bewusst INVOKER, und die Migration bricht mit eigener Post-Condition ab, falls das je kippt. **Nur die gefährliche Richtung ist gesperrt:** `true → false` bleibt jedem Tenant-Admin erlaubt, sonst wäre ein versehentlich gesetztes Flag ohne Service-Role nicht mehr rücknehmbar. **Live-Pentest** `tests/sql/PROJ-Y-146c-audit-exempt-guard-pentest.sql` **A–I 10/10 PASS** gegen Prod, **0 Rückstände** (0 Probe-Mandanten, 5 Mandanten, 4 exempt, Flag-Wechsel-Zähler unverändert 5): A Admin schaltet den **eigenen** Mandanten ab → `42501` + Zustand unverändert · B Wiedereinschalten erlaubt · C Service-Role erlaubt (Runbook-Pfad offen) · D **R-1**: die fünf PROJ-17-Felder (`name`/`domain`/`language`/`branding`/`holiday_region`) gehen unverändert durch, der Trigger feuert dank WHEN-Klausel gar nicht · E Cross-Tenant weiter 0 Zeilen (RLS unberührt) · F Anlage-bereits-ausgenommen blockiert · G `prosecdef = false` · H kein EXECUTE für anon/authenticated · **I Nicht-Leerlauf-Kontrolle**: derselbe UPDATE als `postgres` geht durch — ohne den bewiese A nichts, weil ein Fehlschlag auch von RLS oder einem Tippfehler kommen könnte. **Gegenprobe am Kundenmandanten** (dort war der Angriff ursprünglich vorgeführt): derselbe Aufruf, der vorher 1 Zeile schrieb, endet jetzt in `42501`, Flag bleibt `false`. Advisors **0 ERROR** / 140 WARN (Baseline), kein Treffer auf die neue Funktion. **Ausdrücklich nicht geschlossen:** wer den Service-Role-Schlüssel hält, kann das Flag weiter setzen — das ist der legitime Fixture-Pfad und lässt sich nicht sperren, ohne das 146b-Runbook zu sperren; Meldung dieser Wechsel → **PROJ-Y-146d**. Runbook-Abschnitt „Known gap" entsprechend durch „Who may set the exemption" ersetzt. **PROJ-134-Versionsdrift benign:** die MCP registrierte `20260813152542` gegenüber dem Dateinamen `20260813170000`; die Migration ist durchgängig idempotent (`create or replace function`, `drop trigger if exists` + `create trigger`, wiederholbare Post-Conditions), ein erneutes `db push` liefe folgenlos durch — Datei daher nicht umbenannt, weil ein sekundengenauer Name die Minuten-Raster-Empfehlung des Guards verletzt hätte. **CIA-Verdikt, das den Zuschnitt bestimmt hat:** der ursprünglich geplante Wächter wäre eine *Zustandsabfrage* gegen einen *Transienten* gewesen (Flag an → löschen → Flag aus; ein Nachtlauf sähe vorher und nachher `false`) — deshalb Verhinderung zuerst und der Wächter umgeschrieben statt vorgezogen. **Schwere dabei präzisiert, 6/6 gemessen:** bei gesetzter Ausnahme entfallen **nur** Anlagen und harte Löschungen; Papierkorb (`is_deleted`) und Feldänderungen bleiben protokolliert, weil `record_audit_changes` keinen exempt-Zweig hat. Die Verdeckung war also schon vorher unvollständig. <br><br>**Ausgangsbefund (Analyse, Stand vor der Umsetzung):** **`audit_lifecycle_exempt` war laufend an keine Test-Kennung gebunden.** Gemessen 2026-08-13: kein `CHECK` und kein Trigger auf `tenants` referenziert die Spalte; PROJ-Y-130hs „nichts ohne Test-Kennung ist ausgenommen" war eine **einmalige Migrations-Post-Condition**, keine erzwungene Invariante. **Ausnutzbarkeit live nachgewiesen** (impersonierter Tenant-Admin, zurückgerollte Transaktion, 0 Rückstand): (A) der Admin des **Kundenmandanten** setzt das Flag an der eigenen Zeile — **1 Zeile, `exempt` wird `true`**; (B) dieselbe Sitzung an einem fremden Mandanten korrekt blockiert (0 Zeilen, RLS hält); (C) das Setzen erzeugt **genau 1** Feld-Audit-Zeile, ist also nachweisbar. Grund: `tenants_update_admin` gewährt `is_tenant_admin(id)` ein Zeilen-UPDATE, und **RLS kann nicht auf Spalten einschränken**; `authenticated` hält table-level UPDATE. **Wichtige Präzisierung:** kein Pfad in `src/` schreibt die Spalte (die zwei API-Treffer *lesen* sie über `tenant_audit_lifecycle_exempt`), unsere Oberfläche bietet es also nicht an — das schützt aber **nicht**, weil der Browser den anon-Key trägt und ein Admin mit seinem eigenen JWT direkt gegen die Supabase-REST-API gehen kann. **Optionen, mit Verdikt:** **(1) Trigger, der den Übergang auf privilegierte Rollen begrenzt** — BEFORE UPDATE, feuert nur wenn sich *genau diese* Spalte ändert, `raise 42501` außer für `postgres`/`service_role`. Klein, verhindert statt nur zu erkennen, überlebt künftige Spalten, entspricht dem Hausmuster (Invarianten als Trigger). Preis: ein weiterer Trigger auf einer Kerntabelle; muss belegen, dass legitime Admin-Updates (PROJ-17 Branding/Sprache/Feiertagsregion) unberührt bleiben. **→ empfohlen.** **(2) Spalten-Grant chirurgisch entziehen** — table-level UPDATE entziehen, dann UPDATE auf den übrigen **9** Spalten granten (Postgres kann Spalten-Grants, RLS nicht). Echte Rechte-Ebene, kein Trigger; aber brüchig: jede künftige Spalte ist für `authenticated` stumm nicht schreibbar, bis jemand den Grant nachzieht. Falle. **(3) `CHECK` gegen das `[E2E]`-Namenspräfix** — **nicht empfehlen:** `name` ist vom Admin editierbar, also umbenennen → Flag setzen → zurückbenennen; Speed bump, kein Gate. Blockiert zudem künftige Test-Mandanten mit anderem Namen. **(4) Allowlist-Tabelle** nur per `service_role` pflegbar + Trigger — stark, aber neue Tabelle, neuer Trigger und eine zweite Wahrheitsquelle bei gleichem Effekt wie (1). **(5) Risk-Accept + Wächter**, der exempte Nicht-Test-Mandanten meldet — kostet fast nichts und deckt zusätzlich den `service_role`-Pfad ab, den (1) offen lässt (gewollt: Fixtures brauchen ihn); erkennt aber nur **nachher**. Sinnvoll **als Ergänzung** zu (1), nicht als Ersatz. **Einordnung:** kein Regressionsfehler — PROJ-Y-130h hat das Setzen bewusst auditierbar gemacht und Prävention nie behauptet. Es ist eine Härtung, und sie trifft die Klasse Lücke, gegen die PROJ-130 angetreten ist („auditiert" heißt nur, man sieht es hinterher). **CIA-pflichtig** (Audit-Mechanismus + Trigger auf Kerntabelle) — der Review ist am 2026-08-13 gelaufen und hat Option (1) bestätigt, aber zwei Dinge korrigiert: die Optionsliste stellte mit „welche Mandanten" die falsche Frage, und der als Ergänzung gedachte Wächter (5) hätte in seiner Zustandsabfrage-Form den eigentlichen Missbrauch nicht gesehen. Der Repo-Eigner hat die Umsetzung danach freigegeben. Verlinkt aus PROJ-Y-143o. |
| PROJ-Y-146d | Planned (Followup, 130/146) — **bewusst zurückgestellt, nicht vergessen** | **Melde jeden Wechsel von `tenants.audit_lifecycle_exempt` samt Akteur — als Ereignis, nicht als Zustand.** Diese Zeile existiert vor allem, damit die **naheliegende falsche Fassung** nicht gebaut wird: „melde exempte Nicht-Test-Mandanten" ist eine **Zustandsabfrage** und sieht den relevanten Missbrauch strukturell nicht — der ist ein **Transient** (Flag an → harte Löschungen → Flag aus), und ein Nachtlauf sähe vorher `false` und nachher `false` und meldete nichts. Richtige Fassung: die Feld-Audit-Zeilen (`entity_type='tenants'`, `field_name='audit_lifecycle_exempt'`) **seit dem letzten Lauf** lesen und jeden Wechsel mit Akteur und Mandant melden. Ort: zweiter Schritt im bestehenden 03:45-Siegel-Lauf (`/api/cron/seal-audit-chain`), **kein** neuer Zeitplan. Meldeweg dorthin, wo ohnehin jemand hinsieht — Revisions-Sicht `/revision` bzw. Audit-Bericht (PROJ-130-γ2b/Y-130p) —, nicht in eine Cron-Logzeile. **Warum zurückgestellt:** seit PROJ-Y-146c kann `false → true` nur noch die Service-Role, im Normalbetrieb meldete der Wächter also **unsere eigenen Fixture-Läufe**. Rest-Nutzen ist ein missbrauchter Service-Role-Schlüssel — real, aber dann liegt ein größeres Problem vor als dieses Flag. **Billigere Alternative, zuerst zu prüfen:** eine Teardown-Post-Condition in den committenden Fixture-Skripten (`scripts/verify-prod-snapshot-render.mts` und Geschwister) — „0 exempte Mandanten außerhalb der bekannten Menge", ~10 Zeilen, fängt den Vergessens-Fall an der Quelle statt nachts. |
| PROJ-Y-148a | **Erledigt 2026-08-19 — Variante 1 umgesetzt und deployed** (Tag `v2.63.0-PROJ-Y-148a`) | **Hard-Delete eines Projekts kollidiert mit append-only-Governance-Historie.** PROJ-148 hat die Lead-Ursache behoben (21 → 0 blockierte Papierkorb-Projekte); die verbleibenden **4** scheitern an einer zweiten, unabhängigen Ursache: der `ON DELETE CASCADE` trifft `stakeholder_profile_audit_events` und `decision_approval_events`, deren Guard `UPDATE`/`DELETE` grundsätzlich verbietet (`23514 / … are append-only`). **Das ist kein Trigger-Bug.** Würde man hier denselben „Elternteil wird abgerissen"-Zweig einsetzen wie in PROJ-148, ließe sich Genehmigungs- und Stakeholder-Historie durch Löschen des Projekts tilgen — genau die Klasse Lücke, gegen die PROJ-130 angetreten ist, und ein Rückschritt hinter dessen append-only-Zusage. **Zu entscheiden sind zwei unvereinbare Zusagen:** entweder „endgültig löschen" ist für Projekte mit Governance-Historie **nicht** möglich (dann muss die Oberfläche das ehrlich sagen statt 500 zu liefern), oder die Ereignisse werden beim Projekt-Abriss **entkoppelt statt gelöscht** (Mandanten-Ebene, Muster: `audit_log_entries` hat seinen Mandanten-FK in PROJ-130-α genau deshalb verloren) und unterliegen dann der Aufbewahrung statt der Kaskade. Erste Variante ist billig und ehrlich, zweite ist die saubere. **Nicht nebenbei entscheiden.** Messbar: 4 von 23 Papierkorb-Projekten des Kundenmandanten, live erhoben in zurückgerollter Transaktion. **CIA-Review erbracht 2026-08-18** — Vorlage: [PROJ-Y-148a](PROJ-Y-148a-project-hard-delete-governance-history.md). Zwei Korrekturen an dieser Notiz, beide live erhoben: (1) es sind **fünf** append-only-Inseln im Kaskaden-Abschluss, nicht zwei (`deliverable_approval_events`, `ma_clearance_request_events`, `construction_defect_events` kommen hinzu; PROJ-45-β bringt die dritte in Betrieb); (2) **PROJ-130s Zusage ist strukturell unberührt** — `audit_log_entries` hat 0 Fremdschlüssel, die Kaskade erreicht sie nie; betroffen sind fünf ältere Zusagen (PROJ-31/33/100c/105/45-β), und die Löschung selbst ist über PROJ-130-β ohnehin als `__deleted` protokolliert — verloren ginge der *Detailinhalt* der Inseln, nicht die *Tatsache*. Der dominante Blocker `stakeholder_profile_audit_events` (32 Zeilen, alle vier Projekte) ist **nicht** M&A-spezifisch. **Empfehlung Variante 1** (ehrlich absagen, ~0,5 PT, keine Migration); Variante 2 kostet fünf Zusagen und liefert keine verwertbare Historie (NOT NULL + doppeltes CASCADE → lesbar, aber nicht auflösbar) und löst die DSGVO-Frage nicht, sondern verschärft sie. **Neue, eigens getrennte Lücke:** DSGVO Art. 17 auf `payload` (Big5/OCEAN + Skills, kein Redaktionspfad für die Inseln, Papierkorb unbefristet) → eigener CIA-pflichtiger Followup, hier bewusst **nicht** mitentschieden. **Entschieden + umgesetzt 2026-08-19: Variante 1 (ehrlich absagen).** Keine Migration, kein Guard, kein Fremdschlüssel, keine neue Route — eine Registry der fünf Inseln mit deutschen Fachbegriffen, eine Vorabprüfung am bestehenden `GET …?hard_delete_check=true` und ein 422 `governance_history_immutable` statt des pauschalen 500. **Ein Befund korrigiert die Vorlage:** nur **zwei** der fünf Guards werfen `23514`, die anderen drei werfen `42501` — die von AC-Y148a.V1-1 wörtlich vorgeschlagene SQLSTATE-Abbildung hätte drei Inseln unsichtbar weiter auf 500 laufen lassen (sie haben heute 0 Zeilen); die Vorabprüfung zählt Zeilen und ist deshalb gleichgültig gegenüber dem Fehlercode. **F-13 (hoch, fremdverursacht):** Prod trägt seit 2026-08-14 eine **nicht gemergte Variante 3** (Migration `…projy148a_governance_teardown_tombstone` + `hard_delete_project`-RPC, nur auf dem WIP-Zweig `proj-y-148a/governance-teardown-wip`, von keinem `src/`-Code gerufen) → Repo/Prod-Divergenz, registriert als **PROJ-Y-148c**; AC-Y148a.V1-9 ist dadurch in seiner *Prämisse* überholt (diese Slice fasst nichts an, aber die Guards waren **vorher** schon nicht mehr wörtlich die zugesagten). Live gegen Prod, 0 Rückstände: Löschprobe blockiert → `23514`, unblockiert → gelöscht; Zählabfrage 17/4/0/0/0 identisch zu den SQL-Joins, **Gegenprobe ohne FK-Anker → HTTP 300** (der Anker ist tragend, was kein Unit-Test zeigen kann). Gates: ESLint 0 · tsc 13 = Baseline/0 neu · vitest 3103/3103 · Build clean · Playwright 5/5. **Nicht bewiesen:** kein authentifizierter Browser-Durchlauf des blockierten Pfads — er müsste eine append-only-Zeile in Prod schreiben, die danach nicht entfernbar ist und ihr Projekt dauerhaft unlöschbar machen würde. Q-2 → **PROJ-Y-148b**, Q-3 bleibt offen. **Geschlossen 2026-08-19:** Nutzer entschied **Variante 1**. Der Löschknopf verschwindet bei Governance-Historie, der Dialog benennt die tragenden Inseln samt Zeilenzahl, die Route antwortet `422 governance_history_immutable` statt 500, und die Falschzusage über das Entfernen der Historie ist durch den Sachstand seit PROJ-130-β ersetzt. PR #407 → main `29d0002`, Prod-Deployment 5978173031 `success`, keine Migration. **Eine Auflage des Auftrags war sachlich falsch und wurde korrigiert:** die SQLSTATE-Abbildung auf `23514` hätte drei der fünf Inseln unsichtbar auf 500 gelassen (nur zwei werfen `23514`, drei `42501`) — Autorität ist jetzt eine zählende Vorabprüfung. Scope `full`, weil Variante 2 verworfen und nicht zurückgestellt ist. **Rest ausdrücklich getrennt weitergeführt:** PROJ-Y-148b (DSGVO Art. 17 auf `payload`, CIA-pflichtig), PROJ-Y-148c (Prod trägt eine nie gemergte Variante 3 — Repo/Prod-Divergenz), PROJ-Y-148d (Mängel-Guard von PROJ-45-β weicht ungegated aus). Der blockierte Pfad ist bewusst **nicht** im Browser durchgefahren: der Nachweis würde eine unentfernbare Zeile in Prod schreiben und das Projekt dauerhaft unlöschbar machen. |
| PROJ-Y-148b | Planned — **V1 gewählt 2026-08-19; Rechtsfrage als ADR gestellt, Antwort offen** | **DSGVO Art. 17 auf den `payload` der append-only-Inseln.** Q-2 aus der PROJ-Y-148a-Vorlage, dort ausdrücklich nicht mitentschieden, weil sie eine Rechts- und keine Bedienfrage ist. Lage nach Variante 1: der Papierkorb ist **unbefristet** (kein Auto-Purge, `apply-retention` räumt seit PROJ-130-α nichts), ein Projekt mit Governance-Historie bleibt also dauerhaft liegen — und für die fünf Inseln existiert **kein Redaktionspfad**: `redaction_off` gilt nur für den `audit_log_entries`-Export. `stakeholder_profile_audit_events.payload` trägt Profildaten (Big5/OCEAN, Skills), also genau die Kategorie, für die ein Löschverlangen realistisch ist. **Damit ist „nicht löschbar“ nach dieser Slice ehrlich benannt, aber rechtlich nicht beantwortet.** Die konsistente Richtung ist vorgezeichnet: PROJ-130 hat dieselbe Frage mit **Redaktion statt Löschung** beantwortet (Wert überschreiben, Zeile und Kette erhalten) — eine Löschausnahme würde dagegen genau die Unveränderlichkeit aufheben, die Variante 1 gerade verteidigt hat. Zu klären außerdem, wer redigieren darf (Muster: Admin-Vorbehalt bei `redaction_off`, PROJ-130-γ4) und ob der Vorgang selbst auditpflichtig ist (er ist es). **Nicht nebenbei entscheiden** — CIA-pflichtig, berührt fünf Zusagen und die Hash-Kette aus PROJ-130-ε. **Verschärft am 2026-08-19 durch PROJ-Y-148d:** mit nun **fünf** blockenden Inseln ist der Papierkorb für jedes Projekt mit Governance-Historie endgültig ein Dauerzustand — die Daten bleiben also sicher liegen, und ein Redaktionspfad existiert weiterhin nicht. Die Frage ist damit nicht mehr nur theoretisch. **CIA-Review erbracht 2026-08-19** — Vorlage: [PROJ-Y-148b](PROJ-Y-148b-dsgvo-art17-governance-payload.md). **Zwei Prämissen dieser Notiz halten der Messung nicht stand:** (1) der akute Fall ist **nicht** der Papierkorb, sondern die betroffene Person — ein Stakeholder ist überhaupt nicht löschbar (`23514`, live), **auch ohne** Profil-Historie, weil `decisions.decider_stakeholder_id` (`SET NULL`) gegen `enforce_decision_immutability` läuft; und die Anwendung hat ohnehin **0 DELETE-Handler** dafür. (2) Das hier benannte Vorbild existiert nicht: PROJ-130s Redaktion ist eine **Export-Maskierung**, keine DB-Überschreibung — die „vorgezeichnete konsistente Richtung" wäre neu zu bauen, und ein `UPDATE` auf die Nutzlast ist selbst `23514`. Umfang präzisiert: **vier** Inseln über **drei** Spaltentypen (nur zwei haben `payload`), gemessene Fläche 47 + 10 Zeilen über 9 von 25 Stakeholdern. **Empfehlung V1 → ggf. V3, V2 nicht** — und die **Reihenfolge ist der Kern**: die vorrangige Frage ist Art. 17 Abs. 3 (Aufbewahrung zur Erfüllung rechtlicher Verpflichtungen), also rechtlich; Technik vor dem Rechtsentscheid zu bauen erzeugt einen Löschmechanismus für möglicherweise aufbewahrungspflichtige Daten. Nebenbefund abgetrennt als **PROJ-Y-148f**. **Nutzer-Entscheid 2026-08-19: Variante V1.** Die Frage ist als ADR [governance-history-retention-vs-erasure.md](../docs/decisions/governance-history-retention-vs-erasure.md) gestellt (Status `Proposed — wartet auf rechtliche Feststellung`), nach Art. 17 Abs. 3 lit. b/e formuliert und in **drei Kategorien getrennt** beantwortbar: Genehmigungs-/Freigabe-Historie · Mängel-Historie · Stakeholder-Profile — letztere ist der schwächste Kandidat für eine Ausnahme, weil sie der Steuerung und nicht dem Nachweis dient. Ein gemischtes Ergebnis ist wahrscheinlich. Die Folgen je Antwort stehen im ADR: bei Deckung nur Informationspflicht + AC-Y148b.2 (kein Löschmechanismus), sonst V3 (Anonymisierung am Stammobjekt). **Nächster Schritt liegt beim Verantwortlichen**, nicht in der Architektur. |
| PROJ-Y-148f | Planned (Followup, 148b/20) — **wartet auf den Art.-17-Entscheid** (Nutzer-Entscheid 2026-08-19) | **`decisions.decider_stakeholder_id ON DELETE SET NULL` läuft gegen `enforce_decision_immutability`.** Beim Löschen eines Stakeholders will der Fremdschlüssel `decisions` schreiben, der Unveränderlichkeits-Trigger verbietet jedes `UPDATE` — die Löschung scheitert mit `23514`. Live erhoben in PROJ-Y-148b: **jeder** Stakeholder, der je eine Entscheidung getroffen hat, ist dadurch unlöschbar, **auch ohne** Profil- oder Genehmigungs-Historie. Das ist kein Datenschutz-, sondern ein **Modellierungsbefund**: `SET NULL` auf eine unveränderliche Tabelle ist eine Zusage, die sich selbst widerspricht. **Zu entscheiden:** entweder ist die Sperre gewollt (dann gehört sie ausdrücklich in die PROJ-20-Spec, statt aus einer Fremdschlüssel-Klausel zu folgen, und die Oberfläche muss sie erklären) oder der Verweis wird anders modelliert (z. B. `NO ACTION` mit ausdrücklicher Sperre, oder der Entscheider bleibt als unveränderlicher Wert erhalten statt als Fremdschlüssel). Berührt Invariante #5 und PROJ-20 — **CIA-pflichtig**, nicht nebenbei zu entscheiden. Wirkung heute: 25 Stakeholder in Prod, kein Löschpfad in der Anwendung (0 `DELETE`-Handler), der Widerspruch ist also **latent** und wird akut, sobald PROJ-Y-148b einen Vorgang schafft. **Zurückgestellt bis zum Art.-17-Entscheid (2026-08-19):** die richtige Auflösung hängt davon ab. Wird ein Löschpfad nötig, muss der Widerspruch weg; bleibt es bei Aufbewahrung, genügt es, die Sperre in der PROJ-20-Spec auszusprechen und in der Oberfläche zu erklären — beides ist im ADR [governance-history-retention-vs-erasure.md](../docs/decisions/governance-history-retention-vs-erasure.md) als Folge je Antwort vermerkt. |
| PROJ-Y-148c | **Erledigt 2026-08-19 — zurückgebaut und deployed** (Tag `v2.64.0-PROJ-Y-148c`, Pentest 12/12) | **Prod trägt eine gebaute, nie gemergte und nun verworfene Variante 3.** Erhoben am 2026-08-19 beim Lesen der Guards aus der Live-Definition statt aus der Vorlage: Migration `20260814170000_projy148a_governance_teardown_tombstone` ist seit **2026-08-14** in Prod (registriert `20260814131244`), liegt aber **nicht** auf `main` und **nicht** in `supabase/migrations/` — nur auf dem WIP-Zweig `proj-y-148a/governance-teardown-wip` (Commit `72cfecd`, „Entscheidung offen“). Sie hat (a) alle **fünf** append-only-Guards um einen „Elternteil wird abgerissen“-Ausweg erweitert, vier davon an den Sitzungsschalter `_project_teardown_active()` gekoppelt, und (b) eine RPC `hard_delete_project(uuid)` angelegt: `SECURITY DEFINER`, intern `is_tenant_admin`-gegatet, `EXECUTE` an `authenticated`, schreibt einen `__governance_purged`-Grabstein und reißt dann die Historie mit ab. **Kein `src/`-Code ruft sie** (grep über `src` + `tests`: 0 Treffer), über die Anwendung ist der Weg also unerreichbar — kein offenes Loch, aber eine erreichbare Fähigkeit, die der Nutzer-Entscheid für Variante 1 gerade **verworfen** hat. Zwei Folgen: (1) eine frisch aus den Migrationsdateien gebaute Datenbank hat die Guards **ohne** Ausweg, Prod **mit** — dieselbe Klasse Divergenz wie PROJ-Y-130f; (2) die fünf Unveränderlichkeits-Zusagen (PROJ-31/33/100c/105/45-β) gelten in Prod nicht mehr wörtlich, ohne dass eine Slice das dokumentiert hätte. Zu entscheiden: den Grabstein-Weg **rückbauen** (Migration, die den Ausweg entfernt und die RPC zieht) oder **nachträglich beschließen und einpflegen** (dann gehört die Migration ins Repo und die fünf Specs müssen ihre Zusage ausdrücklich einschränken, Muster AC-Y148a.V2-7). **Bewusst nicht in PROJ-Y-148a mitgetan:** ein Rückbau wäre eine Migration außerhalb des Auftrags, könnte die Arbeit einer Parallel-Session zerstören und würde eine Entscheidung treffen, die dem Nutzer gehört. Am Rand galt derselbe Befund für `construction_defects`/`construction_defect_events` (in Prod, nicht im Repo) — **während der Slice erledigt: PROJ-45-β ist gelandet und hat die Migration nachgeliefert**, dieser Teil der Divergenz ist geschlossen. Offen bleibt allein der Grabstein-Weg. **Geschlossen 2026-08-19 durch Rückbau.** Der Nutzer-Entscheid für Variante 1 machte den Grabstein-Weg gegenstandslos; die vier Guards verweigern wieder ausnahmslos, `hard_delete_project` und `_project_teardown_active` sind entfernt. **Die hier notierte Einschätzung „kein offenes Loch" musste korrigiert werden:** die RPC hatte `EXECUTE` an `authenticated`, und der Produktivmandant hat genau ein Mitglied, das Admin ist — live bewiesen (zurückgerollt) tilgte sie unter den Claims dieses Nutzers Stakeholder-Profil-Historie von 2 auf 0. Erreichbarkeit hängt an `EXECUTE`, nicht daran, was `src/` aufruft. Zweite Divergenz mitgeschlossen (`search_path` bei den PROJ-105-/PROJ-100c-Guards war in Prod gesetzt, in den Dateien nicht). PROJ-45-βs Ausweg bewusst unberührt → **PROJ-Y-148d**. **Rest, der bewusst offen bleibt:** kein Wächter gegen die Wiederholung — der Schema-Drift-Wächter sieht nur `SELECT`-Spalten, nicht Funktionskörper (Klasse PROJ-Y-130f); eigene Slice. |
| PROJ-Y-148d | **Erledigt 2026-08-19 — Ausweg entfernt und deployed** (Tag `v2.65.0-PROJ-Y-148d`, Pentest 6/6) | **`construction_defect_events` verliert seine append-only-Zusage beim Löschen des Projekts — anders als seine vier Geschwister-Inseln.** Gefunden am 2026-08-19 in PROJ-Y-148a, unmittelbar nachdem PROJ-45-β auf `main` gelandet war. `enforce_construction_defect_event_immutability` lässt ein `DELETE` durch, sobald der Eltern-Mangel weg ist — **ohne** die Bedingung `_project_teardown_active()`, an der die vier anderen Guards hängen. Ein Kaskaden-Löschen entfernt die Elternzeile aber zuerst, die Ausnahme greift also bei jedem Projekt-Abriss von selbst. Der Migrations-Kommentar begründet sie damit, der Zweig sei „über die Anwendung ohnehin unerreichbar (keine DELETE-Policy auf `construction_defects`)" — das trifft den direkten Weg, **nicht** den Kaskadenweg `projects → construction_defects → construction_defect_events`, der keine Policy braucht. **Live gemessen** (zurückgerollte Transaktion, 0 Rückstände): ein Gewerk + Projekt-Zuordnung + Mangel + **ein** Ereignis geseedet, dann Projekt gelöscht → `DELETE=SUCCEEDED`, `project_rows_left=0`, **`event_rows_left=0`**. Zu entscheiden: ist das **gewollt** (Mängel-Historie ist weniger schutzbedürftig als Genehmigungs- und Profil-Historie — dann gehört die Abweichung ausdrücklich in die PROJ-45-β-Spec, statt aus einem Kommentar zu folgen, der einen anderen Weg meint), oder ein **übersehener Kaskadenweg** (dann fehlt die `_project_teardown_active()`-Bedingung und der Guard ist in einer Fix-forward-Migration nachzuziehen). **Wirkung heute klein, aber wachsend:** in Prod stehen 0 Mängel-Ereignisse, PROJ-45-β ist gerade erst deployed — die Zahl wächst mit der Nutzung, und rückholbar ist gelöschte Historie nicht. PROJ-Y-148a hat den Befund **nicht** nebenbei behoben (der Guard einer fremden, gerade gemergten Slice ist nicht anzufassen), sondern in seiner Registry als gemessenes `blocksHardDelete: false` abgebildet — sonst hätte die Vorabprüfung Projekte abgelehnt, die sich in Wahrheit löschen lassen. Kippt PROJ-45-β die Entscheidung, ist auf dieser Seite genau ein Boolean zu drehen (plus der Test, der ihn einfriert). **Geschlossen 2026-08-19.** Der Fork ist entschieden, und zwar nicht durch Abwägung: PROJ-45-βs Begründung trifft den **direkten** Weg, nicht die Kaskade (die keine Policy braucht), und ihr zweiter Grund — keinen neuen Blocker der PROJ-148-Klasse anlegen — ist **abgelaufen**, seit PROJ-Y-148a Variante 1 wählte. **Die hier vorgeschlagene Lösung war nicht mehr baubar:** die `_project_teardown_active()`-Bedingung, die nachzuziehen wäre, hat PROJ-Y-148c entfernt. Der Ausweg entfällt daher ganz; alle fünf Inseln sind jetzt gleich behandelt, `blocksHardDelete` steht auf `true` und die Absage nennt „Mängel-Historie". Pentest 6/6 (A: 42501 statt `SUCCEEDED`; B: ohne Mängel weiter löschbar). PROJ-45-βs Vektor Z und Deviation D-β5 sind **nachgezogen** statt stehengelassen — AC-45βH-5 ist damit wörtlich erfüllt. **Rest:** die Verschärfung der DSGVO-Frage (PROJ-Y-148b) ist dort vermerkt, denn mit fünf blockenden Inseln ist der Papierkorb für Projekte mit Historie endgültig ein Dauerzustand ohne Redaktionspfad. |
| PROJ-Y-148e | **Deployed 2026-08-19 — Teil-Wächter, Grenzen gemessen** (Tag `v2.66.0-PROJ-Y-148e`; Enrollment als Required Check = Repo-Eigner-Handoff, weiterhin offen) | **Wächter gegen Prod/Repo-Divergenz bei Funktionsdefinitionen.** PROJ-Y-148c hat einen Einzelfall aufgeräumt (eine nie gemergte Migration lief fünf Tage in Prod und hob vier append-only-Zusagen auf), aber **nichts verhindert die Wiederholung**. Der Schema-Drift-Wächter aus PROJ-42 vergleicht nur `SELECT`-Spalten gegen `information_schema.columns` — er sieht **keine** Funktionskörper, keine Grants und keine Trigger-Verdrahtung. Genau dort saß der Befund. Zu bauen: ein Vergleich der in Prod vorhandenen Funktionsdefinitionen und `EXECUTE`-Grants gegen die aus den Migrationsdateien frisch gebaute Shadow-DB, mit einer geführten Ausnahmeliste für bewusste Abweichungen. **Anspruchsvoll und daher nicht nebenbei:** die Shadow-DB existiert nur im Schema-Drift-Job (Docker, offener WSL-Handoff PROJ-67/F6), Textvergleich von `pg_get_functiondef` ist gegen Formatierungsrauschen empfindlich, und eine zu strenge Fassung würde bei jeder legitimen MCP-Anwendung rot — dieselbe Falle, die PROJ-130-γ1 mit absoluten Zweigzahlen getroffen hat. Verwandt, aber **nicht** dasselbe wie PROJ-Y-130f: das ist ein konkreter Einzelbefund (zwei Audit-Trigger in Prod, die keine Datei herstellt), dies hier ist der strukturelle Wächter. **Gebaut 2026-08-19:** `npm run check:function-inventory` + CI-Workflow (nicht enrolled) + Runbook. **Drei Messungen haben den Zuschnitt erzwungen:** für Funktionen trägt der statische Parse (272 vs. 275, Differenz vollständig erklärbar), für **Trigger nicht** (65 von 74, 14 % Fehlerquote), und ein npm-Skript kann Prod gar nicht befragen (kein Connection-String, PostgREST ohne Systemkataloge) — daher Prüfung gegen ein **versioniertes Inventar** statt gegen die Datenbank. **Die hier notierte Falle ist eingetreten und gelöst:** der erste Lauf war rot wegen einer **laufenden** Slice (PROJ-Y-114a, Migration in Prod, noch nicht gemergt) — das ist der normale Ablauf, also trägt jede Ausnahme ein `kind` (`legacy` / `pending_merge`), und die Liste räumt sich selbst auf, sobald eine Migrationsdatei die Funktion anlegt. **Eigene Fehlschlussfolgerung korrigiert:** die Übereinstimmung meiner 65 mit der 65 aus PROJ-130-α war **Zufall** (Prod führt 74, jene Zahl kam aus einem echten Shadow-DB-Replay) — **PROJ-Y-130f bleibt offen** und braucht Docker. **Rest bewusst offen:** Funktionskörper, Trigger, Grants und Policies bleiben ungeprüft (Shadow-DB → PROJ-67/F6), der Wächter ist blind für alles, was nicht im Inventar steht, und das **Enrollment als Required Check** ist Repo-Eigner-Handoff wie bei PROJ-147. Die stärkere Alternative (Prod-Lesezugang als CI-Secret) ist in der Spec als Entscheidung des Eigners benannt. |
| PROJ-Y-130f | **Erledigt 2026-08-20 — Diagnose deployed** (Tag `v2.67.0-PROJ-Y-130f`); Behebung als PROJ-Y-130g | **Zwei Audit-Trigger existieren in Prod, die die Migrationsdateien nicht herstellen.** Aus dem CI-Replay in PROJ-130-α: die Shadow-DB des Schema-Drift-Wächters erreicht aus den Dateien **65** auditierte Tabellen, Prod hat **67**. *Welche* zwei ist ausdrücklich **nicht** bestimmt — mehrzeilige Trigger-Statements machen einen Datei-Grep untauglich, und die lokale Shadow-DB ist durch den offenen Docker/WSL-Handoff (PROJ-67/F6) blockiert; die Zahl ist belegt, die Zuordnung wurde bewusst nicht geraten. **Warum diese Zeile erst jetzt entsteht:** der Followup war in PROJ-130-α, PROJ-45-β und PROJ-Y-148c **dreifach in Prosa benannt**, hatte aber weder in `INDEX.md` noch hier eine eigene Zeile — dieselbe Klasse Buchführungsfäule, die den ganzen Lauf vom 2026-08-18/19 ausgelöst hat. Inhalt ist aus den drei Erwähnungen übernommen, **nichts ergänzt**. Die Vorbeugung gegen die Klasse ist PROJ-Y-148e. **Korrigiert 2026-08-20 (PROJ-Y-130f).** Die hier notierte Fragestellung trifft die Sache nicht, in beide Richtungen: (1) die Momentaufnahme ist **veraltet** — Prod führt **74** Audit-Tabellen, nicht 67, seit dem 2026-08-11 sind sechs Slices gelandet; eine Differenz „zwei" gibt es nicht mehr. (2) Die Ursache sind **keine Trigger**, sondern **sieben Migrationsdateien, die im Fresh-Apply abbrechen** (`Applied 213 migration(s); 7 tolerated` bei 220 Dateien, Lauf `32278037584`); fehlende Trigger sind eine Folge. **Der Fund saß im Workflow:** der Toleranz-Zweig verwarf die Fehlermeldung und schrieb dabei eine ungeprüfte Ursache fest („REVOKE/GRANT", obwohl nur „function does not exist" geprüft wurde). Die Diagnose ist gebaut — Fehlermeldung, Abbruchzeile, übersprungene Zeilen, Tragweite-Zusammenfassung, Exit-Logik unverändert. **Ein Fall benannt:** `20260428120000_harden_trigger_only_functions.sql`, Zeile 18, `enforce_last_lead` (das PROJ-Y-148e unabhängig als von keiner Datei angelegt gemessen hat) → 18 von 36 Zeilen laufen nicht, eine frische DB lässt `PUBLIC`-EXECUTE stehen, das Prod entzogen hat. **Offen bleibt:** die sechs weiteren Ursachen (liefert der nächste CI-Lauf) und die **Behebung** aller sieben — append-only, also je Fall eine Fix-forward-Entscheidung. Die vollständige Tragweite braucht einen Trigger-/Grant-/Policy-Vergleich gegen die Shadow-DB, also Docker (PROJ-67/F6). **Alle sieben Ursachen liegen jetzt vor** (vom CI-Lauf des eigenen PRs, Summe **883 nicht ausgeführte Zeilen**). Hauptbefund: die zwei größten Verluste (214 + 490 Zeilen) scheitern an der **baren** Form `moddatetime()` — genau die Regel, die CLAUDE.md wörtlich aufstellt; die Extension liegt in Prod in `extensions`, `public` hat 0 solche Funktionen. Und eine Kausalkette verbindet drei der sieben: `proj110` bricht vor Zeile 543 ab, wo es `stage_gate_prereadiness` anlegt, weshalb `proj122` daran scheitert. **Behebung als PROJ-Y-130g** — sie macht den Required Check strenger (490 bisher nie ausgeführte Zeilen), das braucht eine eigene, beobachtete Slice. |
| PROJ-Y-130g | **Erledigt 2026-08-20 — deployed** (Tag `v2.68.0-PROJ-Y-130g`); Rest als PROJ-Y-130q (zunaechst als 130h ausgegeben, ID kollidierte) | **Die sieben Fresh-Apply-Abbrüche beheben.** Daten liegen vollständig vor (PROJ-Y-130f): Datei, Abbruchzeile, fehlende Funktion und verlorene Zeilenzahl für alle sieben, Summe **883 Zeilen**. **Der Hebel ist klein:** drei der sieben — und 767 der 883 Zeilen — gehen auf **zwei** Verletzungen derselben CLAUDE.md-Regel zurück (bare `moddatetime()` statt `extensions.moddatetime` in `proj107_risk_register` und `proj110_stage_gates`; `proj122_spa_issues` folgt als Kaskade, weil `proj110` vor Zeile 543 abbricht, wo es `stage_gate_prereadiness` anlegt). **Weg:** die Migrationen sind append-only und nicht editierbar — stattdessen ein `public.moddatetime`-Stub in der Vorbereitungsphase des Schema-Drift-Workflows, dort wo schon Rollen und Storage-Stubs entstehen. Das ist Test-Infrastruktur und macht die Shadow-DB der echten Supabase-Umgebung ähnlicher, statt eine Migration zu verbiegen. **Warum eigene Slice und nicht Teil der Diagnose:** läuft `proj110` künftig durch, werden **490 bisher nie ausgeführte Zeilen** angewendet. Scheitert eine davon, ist es ein `structural failure` — und der Schema-Drift-Guard ist ein **Required Check**, würde also jeden PR blockieren. Diese Slice muss das erwarten und beobachten; als Nebenwirkung einer Diagnose-Änderung wäre es fahrlässig. Die vier restlichen Abbrüche (`enforce_last_lead` ×2, `decrypt_tenant_ai_key`, `accept_proposal_from_context_undo`) betreffen Funktionen, die keine Datei anlegt bzw. die später gedroppt werden — dort ist je Fall zu entscheiden, ob die verlorenen `revoke`-Anweisungen per Fix-forward nachzuholen sind. Zusammenhang: die zwei `enforce_last_lead`-Fälle sind dieselbe Alt-Divergenz, die PROJ-Y-148e als `legacy`-Ausnahme führt. **Umgesetzt 2026-08-20, mit einer Korrektur am eigenen Vorschlag:** der hier vorgesehene `public.moddatetime`-Stub ist **nicht nötig**. Nachgemessen liegt die Ursache eine Ebene tiefer — der Workflow installiert `moddatetime` bereits korrekt in `extensions`, aber die Shadow-DB lief mit dem Standard-`search_path` **ohne** `extensions`, während Prod `"$user", public, extensions` verwendet. Der Fix ist eine Zeile `alter database ... set search_path` und beseitigt damit eine Umgebungsdifferenz, statt etwas nachzubauen. Dazu wird die CLAUDE.md-Regel ab jetzt **ausdrücklich** erzwungen (`check:migration-naming`, als Fehler, mit eingefrorener Ausnahmeliste) — sonst verlöre sie mit dem Fix ihren zufälligen Erzwinger. Erwartung: 7 → 4 Abbrüche, 883 → ~116 verlorene Zeilen; Ergebnis wird gegen den CI-Lauf gehalten. **Ergebnis:** `Applied 216 migration(s); 4 tolerated abort(s); 0 structural failures` — in **jedem** Punkt wie vorhergesagt, inklusive der Zeilenzahl und der Identität der vier verbleibenden Abbrüche. Das Risiko (704 erstmals ausgeführte Zeilen) hat sich **nicht** materialisiert. Rest als **PROJ-Y-130h** registriert. |
| PROJ-Y-130q | **Erledigt 2026-08-20 — deployed** (Tag `v2.69.0-PROJ-Y-130h`), Scope `full` | **Nach PROJ-Y-130g brechen noch vier Migrationen im Fresh-Apply ab**, mit zusammen **116** nicht ausgeführten Zeilen (von ursprünglich 883): `harden_trigger_only_functions` 18/35 und `proj148_last_lead_cascade_fix` 70/95 an **`enforce_last_lead`**, `security_internal_functions_lockdown` 75/85 an **`decrypt_tenant_ai_key(uuid,text)`**, `proj70_beta_accept_bulk_rpc` 441/505 an **`accept_proposal_from_context_undo(uuid,uuid)`**. **Andere Klasse als die `moddatetime`-Fälle:** kein `search_path`-Problem, sondern `public`-Funktionen, die entweder **keine Migrationsdatei anlegt** (`enforce_last_lead` — dieselbe Alt-Divergenz, die PROJ-Y-148e als `legacy`-Ausnahme führt) oder die eine **spätere Migration droppt** (`decrypt_tenant_ai_key`, `accept_proposal_from_context_undo` — beide tauchten in PROJ-Y-148e als „nur im Repo, nicht in Prod" auf). **Zu entscheiden, je Fall:** ob die verlorenen `revoke`-Anweisungen per Fix-forward nachzuholen sind. Der Inhalt ist bekannt — es sind ausschliesslich `revoke execute`-Zeilen, also Härtung, die in Prod längst angewendet ist und nur im Replay fehlt. Die Wirkung ist entsprechend begrenzt: eine frisch gebaute Datenbank lässt einigen Funktionen `PUBLIC`-EXECUTE, das Prod entzogen hat — relevant für neue Umgebungen und Staging, nicht für Prod. **Nicht kombinieren mit einem Fix an den Migrationsdateien** (append-only); der Weg ist eine neue Migration am Ende, wo alle Funktionen existieren und die `revoke`s durchlaufen. **Umgesetzt 2026-08-20.** Jede der 116 Zeilen gelesen: nur **13 `revoke`s** haben Substanz, der Rest ist Diagnostik und Kommentare. Der naheliegende Fix — alte Anweisungen wiederholen — wäre **falsch** gewesen: `record_tenant_ai_provider_audit` trägt in Prod `authenticated=X` (Nachholen machte die Shadow-DB strenger als Prod), und `accept_proposal_from_context_undo` revoked eine Signatur, die nie existierte. Maßstab ist der gemessene Prod-Zustand. **Zwei eigene Fehler gefangen:** die erste Fassung hätte im Replay einen `structural failure` erzeugt (die zwei `legacy`-Funktionen aus PROJ-Y-148e fehlen dort), und eine Post-Condition prüfte wegen `identity_arguments` statt `oidvectortypes` **nichts** — beide nur durch Nachmessen gegen Prod entdeckt. Die vier **Abbrüche bleiben** (Reihenfolge), geheilt ist der **Endzustand** des Replays. **F-3, vom eigenen CI-Lauf gefunden:** der Fresh-Apply endete mit **0 von 6** RLS-Helfern fuer `authenticated` - nicht durch diese Slice, sondern durch eine Ordnungs-Inversion (Hotfix-Grant sortiert nach Dateinamen vor dem Lockdown-Revoke; in Prod war es 15 Minuten umgekehrt, an der Registry belegt). Wirkung: in jeder frisch gebauten Umgebung scheitert jede Policy, die einen Helfer aufruft. Fix-forward am Kettenende geheilt, in Prod No-op. |
| PROJ-Y-130i | **Erledigt 2026-08-20 — deployed** (Tag `v2.70.0-PROJ-Y-130i`), Scope `full` | **Die Revisions-Freigabe erreichte die mandantenweiten Zweige des Lesetors nicht.** PROJ-130-gamma2 hat `has_audit_reader_grant` an EINER Stelle verdrahtet - dem gemeinsamen Ausgang von `can_read_audit_entry`; Zweige, die vorher zurueckgeben, erreichen ihn nicht. **Zwei Messungen haben den Zuschnitt geaendert:** die Notiz sagte „9 Einzel-Ersetzungen", live sind es **10** (`construction_trades` kam mit PROJ-45-alpha nach); und die registrierte Klasse traegt in Prod nur **6** Audit-Zeilen, waehrend die NICHT registrierte Klasse `return false` **126** traegt - darunter die **5 Umschaltungen von `audit_lifecycle_exempt`**, also genau den Schalter, den PROJ-Y-130h auditpflichtig gemacht hat, damit „wer die Ausnahme setzt, seine eigene Spur nicht verwischen kann". Lesen durfte ihn nur die Mandanten-Administration - dieselbe Gruppe, die ihn setzt. **Nutzer-Entscheid:** Klasse A plus die mandantenweite Konfiguration (`tenants`, `tenant_settings`, 2 Overrides), **ohne** `resources` (display_name + Tagessaetze, und die Redaktion existiert nur im Export und nur fuer `stakeholders`) und **ohne** die zwei Skill-Kataloge (die haengen an `is_tenant_admin`; die Freigabe ersetzt die Mitgliedschaft, nicht die Rolle). Klasse A bekommt `member OR grant`, die Konfiguration **nur** `grant` - ein `OR member` haette jedem Mitglied die Konfigurationshistorie geoeffnet. Live-Pentest **33/33 PASS, 0 Rueckstaende** unter einem Nicht-Mitglied (unter Admin waere er falsch-gruen); tragend sind die negativen Zeilen: `resources` und beide Skill-Kataloge bleiben auch mit Freigabe zu, ein Mitglied ohne Freigabe sieht die Konfiguration nicht. Regressionen **gamma2 11/11 woertlich**, **gamma1 11/11**. **F-1 (Bestandsbefund):** gamma1s Vektor G2 nagelte „genau 60 when-Zweige" und war rot bei 64 - nicht durch diese Slice (Messung vor der Migration ergab schon 64), die vier Zweige stammen aus PROJ-45-beta/gamma und PROJ-80-alpha1, namentlich belegt. Die Datei hatte genau das vorhergesagt; G2 ist jetzt eine Untergrenze plus **G2b** mit 30 namentlich gepruefen Geschwistern, rot-gruen belegt. Folge-Frage `resources`/Redaktion -> **PROJ-Y-130r**. |
| PROJ-Y-130r | Planned (Followup, sicherheitsrelevant) | **Die Class-3-Redaktion im Audit-Trail deckt nur eine Tabelle und nur eine Flaeche ab.** Gemessen in PROJ-Y-130i: `[redacted:class-3]` existiert ausschliesslich in `src/app/api/audit/export/route.ts` und greift nur fuer `entity_type = 'stakeholders'` mit fuenf Feldnamen (`CLASS_3_STAKEHOLDER_FIELDS`); die **Berichts**-Route und der **Verlaufs**-Tab redigieren **gar nicht**. Wirkung heute: wer eine Flaeche legitim sehen darf, sieht dort auch Klartext - z. B. `resources.display_name` und Tagessaetze fuer Projektmitglieder. Genau deshalb hat PROJ-Y-130i den `resources`-Zweig fuer Revisoren bewusst **zu** gelassen, statt die Redaktion nebenbei zu erweitern. Zu klaeren: eine Redaktionsschicht an EINER Stelle (die drei Leseflaechen teilen keine), welche entity/field-Paare Class-3 sind (der Klassifizierer aus PROJ-12 kennt Felder, das Audit kennt Namen), und ob `redaction_off` als Admin-Vorbehalt (gamma4) die richtige Linie bleibt. CIA-pflichtig: Sicherheitsflaeche und produktweite Wirkung. |
| PROJ-108 | Superseded by PROJ-114 | Red-Flag-Log absorbed by deployed `dd_findings`. AC4 report→PROJ-116; AC3 valuation/SPA→PROJ-120/121/122; AC1-gap→**PROJ-Y-114a** (vormals `PROJ-Y-1`, umbenannt 2026-08-18 wegen Fuenffach-Kollision); lens→PROJ-Y-2; PMI-handover→PROJ-125/127. No own build. |
| PROJ-Y-144d | **Resolved 2026-08-12** (shipped inside PR #341) | **Done.** Dedicated assistant E2E tenant (`E2E_ASSISTANT_TENANT_ID`) with the module explicitly active plus a **Scrum** project, seeded idempotently in `global-setup`; active tenant now pinned via the `active_tenant_id` cookie in `auth-fixture.ts` (the user has two memberships, and the cookie-less fallback picks the *earliest* one — a coin flip on a fresh seed, which would have put every existing `authenticatedPage` spec one race from another workspace). New spec `tests/PROJ-Y-144d-assistant-work-item-chain.spec.ts` **3/3 chromium**: the load-bearing case asserts that **before** the confirmation the draft exists and the work item does not, then corrects the title and confirms into exactly one `story` carrying the corrected text; a second case proves resumption across a **real reload** (Escape only proves React state survived); a third case guards the other lane by asserting the launcher is absent on the shared tenant. Residue 0 across all five assistant tables. Non-regression measured at an identical commit: the authenticated visual spec is 7 failed / 2 passed on pristine `origin/main` **and** on the branch. Delivered in the PROJ-144 PR rather than separately, so PROJ-144 never sat on `main` with an unproven acceptance criterion. Findings F-9/F-10/F-11 recorded in the PROJ-144 spec. Original blocking analysis kept below for the record: all three assistant surfaces are module-gated and the shared E2E tenant has the assistant module **off** (`assistant_aktiv = false`, verified live), its project has no method (`project_method = null`), and `AssistantLauncher` sits in `src/components/app/app-shell.tsx:57` — a `fixed` button on **every** signed-in page, exactly the shell `tests/PROJ-51-visual-regression-authenticated.spec.ts` captures `fullPage`. Switching the module on in the shared tenant would alter the 7 baselines PROJ-Y-143b/d just stabilised, and toggling it inside a spec would flake against concurrently running visual specs (Playwright parallelises files). Recommended shape: a second E2E tenant with the module active + a project carrying a method, so visual baselines stay untouched. Until then PROJ-144 proves the confirmation **mechanics** (DB pentest 17/17, route tests 11 cases, component tests 6 cases) but **not** the browser chain. Same lesson as PROJ-135: an unrun E2E layer is an open acceptance criterion, not a deviation. |
| PROJ-Y-144a | Planned (Followup, 144) | LLM-Extraktion für freies Diktat — neuer AI-Purpose, der aus unstrukturierter Rede Titel/Art/Beschreibung ableitet. **Keine deferierte Original-Anforderung:** PROJ-144 hat den regelbasierten Weg als Lock L3 gewählt, gerade um die volle Purpose-Pflicht zu vermeiden (CHECK-Lockstep in `ki_runs` + `tenant_ai_cost_caps`, alle fünf Cloud-Provider nach PROJ-85, `reason_code` nach PROJ-137) und um ohne tenant-eigenes Ollama nicht in den PROJ-86-Fall „0 Vorschläge" zu laufen. Erst bauen, wenn Pilot-Feedback zeigt, dass die Kommandoform zu eng ist. |
| PROJ-Y-144b | Planned (Followup, 144) | Mehrfachanlage aus einem Diktat („drei Stories: …"). Bewusst außerhalb von PROJ-144, dessen Bestätigungsfluss genau ein Work-Item pro Entwurf beansprucht (Doppelklick-Schutz D5: der Entwurf wird beansprucht, *bevor* das Work-Item entsteht). Mehrfachanlage braucht ein eigenes Beanspruchungs- und Teilfehler-Modell. |
| PROJ-Y-143o | **Erledigt 2026-08-14 — deployed** (Tag `v2.58.0-PROJ-Y-143o`, PR #391), Scope `tooling-only` | **E2E-Projektanhäufung an der Quelle stoppen.** PROJ-Y-143c hat 43 Testprojekte aus dem Alt-Mandanten geräumt, aber nur das Symptom: der aktuelle Fixture-Mandant `e2e00000-…-0002` trägt bereits **20 Projekte** (10 × „[E2E 135] Finalize Project", 9 × „[E2E ε] Wizard KI Project", 1 gewollte Fixture) — in drei Tagen entstanden, weil die Wizard-Finalize-Specs nichts aufräumen. Zweite Schadensstufe live belegt: `tests/sql/PROJ-77-gamma-…` hatte eines der angesammelten Projekte als Fremdmandanten-Fixture **hart verdrahtet** und brach beim Aufräumen mit `23503` — angesammelter Testmüll wird stillschweigend zur Fixture, auf die sich Sicherheitsnachweise verlassen. Richtungsentscheid offen (Teardown je Spec vs. Wiederverwendung vs. Sweep in `global-setup`); `enforce_last_lead()` blockiert das naive Löschen. **Wechselwirkung PROJ-Y-143l:** legt parallel eine dritte Fixture-Lane an (Mandant `…0007`, seit 2026-08-13 09:53 UTC) — Lösung muss über alle Fixture-Mandanten tragen, daher erst 143l abwarten. Die 20 Bestandszeilen werden bewusst **nicht** angefasst. **Stand-Nachtrag 2026-08-20:** diese Registerzeile war **stale** — sie beschrieb noch den Zustand vor der Arbeit („Richtungsentscheid offen … erst 143l abwarten"), waehrend der INDEX die Slice seit dem 2026-08-14 als `Deployed`/`tooling-only` fuehrt. Gefunden beim Erheben des Gesamtstands, dieselbe Klasse Widerspruch, mit der diese Sitzung begann (PROJ-Y-2: INDEX `In Review`, Register „PR offen"). **Und der Richtungsentscheid ist nicht offen geblieben, sondern anders ausgefallen als hier notiert:** die Annahme „die Specs raeumen nicht auf" stimmte nicht - alle drei hatten ein `afterAll`, das nur wirkungslos war (falsche Tabelle `project_members` statt `project_memberships`, danach `23514` aus PROJ-148), und supabase-js wirft bei DB-Fehlern nicht, sodass jeder Teardown den Fehler verschluckte. Umgesetzt wurde daher keine der drei registrierten Varianten, sondern: falsche Tabelle raus, Projekt-Delete **laut** ueber `deleteOrThrow`, und `audit_lifecycle_exempt` in allen drei Fixture-Mandanten-Upserts von `global-setup`. Nachweis: beide projekt-erzeugenden Specs 9 passed, `projects` im Fixture-Mandanten **20 vorher → 20 nachher**. |
| PROJ-Y-144c | Planned (Followup, 144) | Eltern-/Sprint-Zuweisung per Sprache. PROJ-144 legt bewusst auf oberster Ebene an und erbt die Elternregeln der bestehenden Work-Item-Route; Zuweisung per Sprache braucht Auflösung gesprochener Eltern-/Sprint-Namen und damit einen eigenen Erkennungspfad. |

| PROJ-Y-98a | Planned (Followup, 98) — **neu vergeben 2026-08-20** | **Stage-Gate-Zuordnung je Gremium** (`committee_stage_gates` M:N). Quelle: **AC2** von PROJ-98 (strukturierter Entscheidungs-/Eskalations-Bezug statt Freitext). Die Spec nennt dieses Followup „PROJ-Y-1" — der Name ist mehrfach belegt (siehe Bookkeeping-Konflikt „PROJ-Y-1/2/3"), deshalb diese eindeutige ID. Vorbedingung PROJ-110 ist seit 2026-07-21 deployed, das Followup ist also **jetzt baubar**. |
| PROJ-Y-98b | Planned (Followup, 98) — **neu vergeben 2026-08-20** | **„Nächste Termine" in der Gremienübersicht** + Protokoll-Verknüpfung. Quelle: **AC3 + AC5** von PROJ-98 (in der Spec „PROJ-Y-2"). PROJ-117 hat Termine, Protokolle und den Beschluss-Übertrag geliefert; die **Gremienübersicht selbst** zeigt weiterhin nur einen „Termine"-Knopf je Gremium, keine nächsten Termine — der Rest der AC ist damit offen, nicht erledigt. |
| PROJ-Y-98c | Planned (Followup, 98) — **neu vergeben 2026-08-20** | **Standard-Gremien aus dem Projekt-Template seeden.** Quelle: **AC4** von PROJ-98 (in der Spec „PROJ-Y-3"). PROJ-96 liefert den Template-Katalog, PROJ-117 einen eigenen Gremien-Vorlagen-Katalog mit 6 Typen — die Brücke „Projekt-Template legt Gremien an" fehlt. |
| PROJ-Y-98d | Planned (Followup, 98) — **neu vergeben 2026-08-20** | **Eskalations-Bezug Gremium zu Entscheidung/Eskalation.** Quelle: **AC2/AC5** von PROJ-98 (in der Spec „PROJ-Y-4"). PROJ-111 hat `decision_body` als Feld, die Eingabe im generischen Entscheidungs-Formular fehlt (siehe PROJ-111 AC1-Lücke) — beide Followups gehören zusammen. |
| PROJ-Y-99a | Planned (Followup, 99) — **neu vergeben 2026-08-20** | **Externe Aktivitäten im Audit-Trail separat markieren und filterbar machen.** Quelle: **AC4** von PROJ-99, in der Spec ausdrücklich als „Offen" geführt: geliefert ist der Feld-Audit auf Advisor-/NDA-/Clearance-Objekten, **nicht** die markierte Spur über Logins, Datenzugriffe und Downloads. Teilweise überholt: PROJ-130-δ1/δ2 protokollieren inzwischen Download-Link-Ausgabe, CSV-Exporte und `strict`-Leseflächen — **ohne** Externen-Marker und ohne Filter. Der Rest der AC ist damit weiterhin offen. |
| PROJ-Y-100c-a | Planned (Followup, 100c) — **neu vergeben 2026-08-20** | **Magic-Link für externe Approver** im 4-Augen-Gate. Quelle: **AC-100c-8**; der Approver-Pool ist konto-basiert (`approver_user_id`), externe Genehmigung braucht eine Token-Respond-Route nach dem PROJ-31-Muster. Die Spec führte das als „PROJ-Y-Kandidat" ohne ID. |
| PROJ-Y-128a | Planned (Followup, 128) — **neu vergeben 2026-08-20** | **NDA-Wiedervorlage 30 Tage vor Ablauf.** Quelle: **AC2** von PROJ-128, in der Spec als „Offen" geführt: `reminder_date` existiert als Feld, der auslösende Lauf nicht. Bewusst harmlos: das Zugriffs-Gate prüft `status`/`valid_until` direkt und hängt **nicht** am Reminder. |
| PROJ-Y-129a | Planned (Followup, 129) — **neu vergeben 2026-08-20** | **„Public" als Export-/Publikationsstufe + Stufenkennzeichnung auf Exporten.** Quelle: **AC2 + AC5** von PROJ-129. Bewusste MVP-Reduktion auf drei geordnete Stufen (eine vierte hätte alle Policies, Tests und M&A-Adopter angefasst); Wasserzeichen auf Exporten war eine offene Frage und ist nicht gebaut. Berührt PROJ-Y-130r (Redaktion deckt nur eine Tabelle und eine Fläche ab). |
| PROJ-Y-145h | Planned (Followup, 145) — **Buchführungshygiene** | **Mehrfach belegte Followup-Kennungen `PROJ-Y-1`, `PROJ-Y-2`, `PROJ-Y-3` eindeutig machen.** Beim Tranche-3-Audit gemessen: `PROJ-Y-1` bezeichnet **vier** verschiedene Dinge (Register: DD-Finding-Herkunftsnachweis, PR #400 offen · PROJ-98: Stage-Gate-Zuordnung · PROJ-114: E-Mail-/Teams-Eskalation · PROJ-116: Word-Export), `PROJ-Y-2` ebenfalls vier (Register: Red-Flag-Lens, **deployed** · PROJ-98: Termine · PROJ-114: 4-Augen für Deal-Breaker · PROJ-116: Snapshot-Freeze), `PROJ-Y-3` drei. Konkrete Gefahr: das Register führt `PROJ-Y-2` als erledigt, während drei gleichnamige Followups offen sind. Tranche 3 hat für PROJ-98 die eindeutigen IDs 98a–98d vergeben; 114 und 116 sind noch umzubenennen (Specs anderer Lanes, deshalb eigene Slice). |

## New Feature Series

The new Skill/DMS/RAG feature specs originally collided with reserved PROJ-71..75 IDs. They are now reconciled as PROJ-76..84.

| PROJ | Feature | Dependency assessment |
|---|---|---|
| PROJ-76 | Skill-Framework Foundation | Independently buildable. Requires only deployed PROJ-2, PROJ-4, and PROJ-10. Best first product slice from the new series. |
| PROJ-77 | Skill-Customizing | Not independent. Requires PROJ-76 and PROJ-79 because knowledge links target DMS nodes. |
| PROJ-78 | Skill-Projektzuordnung | Not independent. Requires PROJ-76; can follow as a smaller wizard/sidebar slice. |
| PROJ-79 | DMS Foundation | Independently buildable but larger than PROJ-76. Requires only deployed PROJ-2, PROJ-4, and PROJ-10; should reuse PROJ-70 parser/storage hardening without merging with `context_sources`. |
| PROJ-80 | RAG-Indexierung + Quintessenz | Not independent. Requires PROJ-79 and PROJ-76; must reuse PROJ-70 `pdfjs-dist`/`mammoth` hardening decisions. |
| PROJ-81 | Skill-to-RAG-Scope | Not independent. Requires PROJ-76/77/78/79/80. |
| PROJ-82 | Skill-driven AI Proposals | Not independent. Requires PROJ-76/77/78/80/81 plus PROJ-12; persistence target is `ki_suggestions`, not an `ai_proposals` table. |
| PROJ-83 | Task-driven Content Generation | Not independent. Requires PROJ-76..82 and DMS persistence from PROJ-79. |
| PROJ-84 | KI-Kennzeichnung + erweiterter Audit-Trail | Cross-cutting. Should follow at least PROJ-80/82/83 emitters; complements PROJ-12 `ki_provenance` instead of replacing it. |

## Deployed With Deferreds Or Partial Scope

| PROJ | Status note |
|---|---|
| PROJ-130 | **Deployed / Scope `mvp`** (klassifiziert 2026-08-12 durch PROJ-145). Alle fünf Sub-Slices α–ε sind live und per Live-Pentest gegen Prod verifiziert; `full` ist ausgeschlossen, weil **AC-5 („konfigurierbare Speicherdauer") per PO-Lock umgekehrt statt erfüllt** wurde — die Spec sagt das selbst so, und die Umkehr (unbegrenzte Aufbewahrung) ist produktlich die stärkere Wahl, formal aber ein offenes Kriterium. Weitere benannte Omissionen: kein Legal-Hold (bewusste Abweichung), kein WORM-Export (Spec-Frage mit „nein" beantwortet), Restabdeckung der 132 mandantenbezogenen Tabellen → PROJ-Y-130d. Übrige Followups PROJ-Y-130a/b/c/e–o. Ein Upgrade auf `full` verlangt laut Regel einen neuen QA-/Deploy-Durchlauf und würde bedeuten, AC-5 zu erfüllen oder die Anforderung selbst zu ändern — nicht, die Bewertung nachzuziehen. |
| PROJ-145 | **Deployed / Scope `tooling-only`** (2026-08-12, Tag `v2.55.0-PROJ-145`). **Bewusste Teil-Lieferung.** Die Slice führt die `Deployment Scope`-Spalte ein und bucht die zwei verifizierten Zeilen, klassifiziert aber **141 Altzeilen absichtlich nicht** (Nutzer-Lock: Schuld sichtbar lassen statt raten; die Regel verbietet Rückschlüsse aus dem alten `Deployed`-Etikett). Der Marker `– nicht klassifiziert –` ist kein Scope-Wert, sondern ein gezählter Übergangszustand — `npm run check:index-scope` weist die Restzahl bei jedem Lauf als Warnung aus. Auflösung → PROJ-Y-145b. |
| PROJ-Y-143c | **Deployed / Scope `tooling-only`** (2026-08-13). Option A ausgeführt: 43 Projekte + 42 `project_memberships` im Alt-Mandanten gelöscht, Mandant/Nutzer/Settings/Kontextquellen/Storage/41 Audit-Zeilen bewusst behalten (vier Pentests brauchen den Mandanten als Fremdmandanten). Option B verworfen — sie hätte 4 Pentests gebrochen und **48 neue, unlöschbare Audit-Zeilen** erzeugt, weil die `tenants`-Zeile beim Kaskadieren schon weg ist und `audit_lifecycle_exempt` dadurch nicht mehr greift. **Eine akzeptierte Auslassung aus einer Original-Anforderung: AC-Y143c.4** verlangt neben den Pentests auch die authentifizierte Playwright-Suite; gelaufen sind die **vier** mandantenbezogenen Pentests (PROJ-78 14/14, PROJ-Y-96b 9/9, PROJ-77-γ 7/7 nach Fix, PROJ-96 5/6 mit vorbestehendem Harness-Defekt), die Playwright-Hälfte **nicht**. Begründung statt Erledigt-Behauptung: PROJ-143 hat alle E2E-Fixtures auf `…0002`/`…0004` umgestellt und der Grep über alle 43 gelöschten IDs ergibt außerhalb einer einzigen SQL-Datei null Treffer — ein starkes Argument, aber kein Testlauf. Nachzutragen von der nächsten Session, die die Suite ohnehin fährt. Ursache der Halde bleibt offen → PROJ-Y-143o. |
| PROJ-45 | **Deployed / Scope `alpha`** — zwei Sub-Slices live: **α** Gewerke & Bauabschnitte (2026-08-14, Tag `v2.56.0-PROJ-45-alpha`) und **β** Mängelmanagement (2026-08-19, Tag `v2.61.0-PROJ-45-beta`). **Der Eintrag fehlte hier seit dem α-Deploy** und ist mit dem β-Deploy nachgetragen — die Auslassungen selbst waren durchgehend oben unter „Planned Followups“ geführt, nur die Klassifikation nicht. `full` ist zweifach ausgeschlossen: drei Stories der Erstfassung sind zurückgestellt (**γ** Abnahmen ST-02, **δ** Terminsignale ST-04, **ε** Fotodokumentation) **und** aus β die Original-Anforderung **AC-45β.18** (überfällige Mängel in der PROJ-103-Engpass-Sicht, nach δ verschoben, D-β1). `alpha` statt `mvp`, weil beide Slices benannte Sub-Slices mit eigenen Akzeptanzkriterien, eigener QA und eigener Deploy-Evidenz sind und die Restslices namentlich geführt werden. Offene Kleinbefunde: **PROJ-Y-45b** (roher 500-Pfad der Entfernen-Routen für künftige Verweise), **PROJ-Y-45d** (unkontrollierte Auswahlliste, Konsolenlärm), **PROJ-Y-45e** (Feld-Audit vom Ausnahmeflag nicht gedeckt). |
| PROJ-7 | Deployed MVP slice: WIP limits, velocity/burndown, resource swimlanes, phase gates, and tenant health thresholds remain deferred or assigned elsewhere. |
| PROJ-14 | Deployed plumbing slice: real adapters are separate followups PROJ-47/48/49/50. |
| PROJ-18 | Deployed: template UI, platform-default tag rename, insert provenance shape, and phase-close UX refinements remain deferred. |
| PROJ-23 | Deployed, partial: server-side viewport default not implemented; mobile handled client-side. |
| PROJ-25/25b | Deployed: backlog-sprint DnD closed by 25b; Gantt followups such as auto-schedule, touch polish, realtime cursors, undo, and critical-path math remain deferred. |
| PROJ-28 | Deployed: global redirect kill-switch + structured Sentry breadcrumb shipped 2026-06-07; tenant-scoped staged-rollout flag and full logged-in Playwright matrix remain deferred. |
| PROJ-37..41 | Deployed assistant core MVP: wake-word, external speech providers, always-listening/autonomous workflows, and full cross-browser voice validation remain deferred. |
| PROJ-42 | Deployed α: INSERT/UPDATE/Zod drift and shadow-vs-prod drift remain deferred. |
| PROJ-44 | Deployed α+β foundation: γ classifier deferred; δ/ε superseded by PROJ-70. |
| PROJ-53 | Deployed α+β: memo split, custom calendars, multi-locale, PNG/PDF export intentionally deferred. |
| PROJ-54 | Deployed α+β+γ: versioned `resource_rate_overrides` table deferred. |
| PROJ-62 | Deployed: module-active API gate, critical move-confirm, UX polish, and E2E still open. |
| PROJ-63 | Deployed: intentionally narrow CSV importer; dynamic mapping, XLSX, Entra-ID sync, free-form detection, vendor import, and incremental re-imports deferred. |

## Tranche 3 (PROJ-Y-145b) — M&A-Familie: Scope je Zeile und Quell-AC der Auslassung

Erhoben 2026-08-20 gegen Spec, Akzeptanzkriterien, QA-Verdikt und Deployment-Notiz — **nicht** aus dem alten
`Deployed`-Etikett. 35 Zeilen: **13 `full`**, **21 `mvp`**, **1 `alpha`**. Die Begründung steht zusätzlich als
Zitat direkt unter dem `## Deployment Scope`-Header der jeweiligen Spec, damit sie dort auffindbar ist, wo sie geprüft wird.

Wiederkehrendes Muster dieser Familie: die Slices wurden als **verzahnte Kette** gebaut, in der jede Slice Teile
an ihre Nachfolger abgegeben hat. Viele dieser Lücken sind inzwischen von einer späteren, eigenständig deployten
Slice geschlossen. Das hebt den Scope hier **nicht** automatisch — die Hausregel verlangt für `alpha → mvp → full`
einen eigenen QA-/Deploy-Pass. Wo das zutrifft, ist es unten als „Upgrade-Kandidat" vermerkt.

| PROJ | Scope | Quell-AC der Auslassung | Ziel-Followup |
|---|---|---|---|
| PROJ-94 | full | — | — |
| PROJ-95 | mvp | AC-95-5 teilweise (Deliverable-Verknüpfung an der Phase) | PROJ-104 — **geliefert**, Upgrade-Kandidat |
| PROJ-96 | alpha | AC2 partial (Custom-Create/Copy/Deep-Version-Edit), DoD-Versionierung | PROJ-Y-96c / PROJ-Y-96d — offen |
| PROJ-97 | full | — | — |
| PROJ-98 | mvp | AC2 (strukturierter Gate-/Eskalations-Bezug), AC3, AC4, AC5 | PROJ-Y-98a / 98b / 98c / 98d — offen, IDs neu vergeben |
| PROJ-99 | mvp | AC4 (Externen-Marker im Audit-Trail, filterbar) | PROJ-Y-99a — neu vergeben |
| PROJ-100 | full | AC2/AC4/AC5 an 100b/100c abgegeben, **beide vor der Closure deployed** | — |
| PROJ-100b | full | — | — |
| PROJ-100c | mvp | AC-100c-8 (Magic-Link externe Approver) | PROJ-Y-100c-a — neu vergeben |
| PROJ-101 | mvp | AC2 halb (Links zu Risiko/Entscheidung/Deliverable) | PROJ-Y-101b — offen |
| PROJ-102 | mvp | AC4 + AC5 ganz, AC2/AC3 teilweise | PROJ-Y-102c / 102d — Ziel-Slices 96/104/131/132 geliefert, Upgrade-Kandidat |
| PROJ-103 | mvp | AC3 (Top-3 **auf dem Projekt-Dashboard**) | PROJ-Y-103a — offen |
| PROJ-104 | mvp | AC3 (Deliverable aus Template vorbelegbar) | PROJ-Y-104a — Ziel-Slice PROJ-96 geliefert, Upgrade-Kandidat |
| PROJ-105 | full | — (α/β ist Auslieferungsreihenfolge, kein offenes Kriterium) | — |
| PROJ-106 | full | — | — |
| PROJ-107 | mvp | AC5 („im Reporting"), AC1 Kategorie-Pflicht nur clientseitig | PROJ-Y-107a — offen |
| PROJ-109 | full | — | — |
| PROJ-110 | mvp | AC1 (Custom-Gates), AC2 (Pflicht-Deliverables im Pre-Read) | PROJ-Y-110-quorum bzw. PROJ-104 — offen |
| PROJ-111 | mvp | AC1 (Gremium/Optionen ohne FE-Eingabe), AC2 (Deliverable-Link), AC5 (UI-Filter) | PROJ-Y-111a — offen |
| PROJ-112 | mvp | AC2 (Pflicht-Deliverables, Prüfpunktliste), AC4 (Zähler), AC5 (Gate-5) | PROJ-Y-112a / 112b — Ziel-Slices geliefert, Upgrade-Kandidat |
| PROJ-113 | mvp | AC4 (Frage zu Finding eskalieren) | PROJ-Y-113c / PROJ-Y-114a — **offen, PR #400** |
| PROJ-114 | mvp | AC2 teilweise (Q&A/SPA/Bewertung), AC5 teilweise (Datei-Export, DD-Bericht) | PROJ-116 + PROJ-Y-114a — teils geliefert |
| PROJ-115 | mvp | AC2 (aktive Erreichbarkeitsprüfung) | PROJ-Y-115a — offen |
| PROJ-116 | mvp | AC1 teilweise (Pflicht-Deliverables im DD-Bericht) | PROJ-Y-3-„D1" — **geliefert mit #292**, Upgrade-Kandidat |
| PROJ-117 | full | — (AC5 ist in der Spec ausdrücklich „optional") | PROJ-Y-117a als Erweiterung |
| PROJ-118 | full | — | PROJ-Y-118a als Erweiterung |
| PROJ-119 | full | — | — |
| PROJ-120 | mvp | AC3 teilweise (Synergie-Hälfte, PROJ-126 existiert nicht) | PROJ-126 — nicht gebaut |
| PROJ-122 | mvp | AC-3 teilweise (Kaufpreis-Bridge, Closing Conditions) | PROJ-Y-122a — offen, PROJ-121/123 `Planned` |
| PROJ-128 | mvp | AC2 (Wiedervorlage 30 Tage vor Ablauf) | PROJ-Y-128a — neu vergeben |
| PROJ-129 | mvp | AC2 (vierte Stufe „Public"), AC5 (Stufe auf Exporten) | PROJ-Y-129a — neu vergeben |
| PROJ-131 | full | — | PROJ-131-β / PROJ-Y-131a / 131b als Erweiterungen |
| PROJ-132 | full | — | — |
| PROJ-Y-96b | full | — | PROJ-Y-96d als Erweiterung |
| PROJ-Y-96e | mvp | AC8 halb (Estimated-Days-Passthrough, Zielspalte fehlt) | PROJ-Y-96e-e1 — offen |

## Bookkeeping Conflicts

| PROJ | Resolution |
|---|---|
| PROJ-1 | Do not count as open: domain-claim browser smoke plus invite/role/revoke live service-role and multi-user smokes added in `tests/PROJ-1-2-live-closure.spec.ts`; the spec builds its own service-role client with an explicit `ws` transport (Node-20 realtime workaround, same pattern as the PROJ-70/88/89/90 specs) — no production admin-client change required. |
| PROJ-2 | Do not count as open: hard-delete live route smoke added in `tests/PROJ-1-2-live-closure.spec.ts`; optimistic concurrency remains an explicit P1 follow-up in the PROJ-2 spec, not a partial deployment blocker. |
| PROJ-36 | Do not count as open: Index/spec header say β was absorbed by PROJ-9-R2; spec bookkeeping cleanup added 2026-06-07. |
| PROJ-34 | Do not count as open: old QA partial blocks are historical; Index/spec header say status closure including F-9 audit-restore is live; spec bookkeeping note added 2026-06-07. |
| PROJ-64/65 | Do not count as open: old partial blocks were later closed. |
| PROJ-71..75 | Reserved for PROJ-70 followups; new Skill/DMS/RAG specs moved to PROJ-76..84. |
| PROJ-130 + PROJ-144 | **Resolved 2026-08-12 durch PROJ-145.** Beide sind gebucht: PROJ-130 `Deployed`/**`mvp`** (AC-5 per PO-Lock umgekehrt statt erfüllt, kein Legal-Hold, Restabdeckung → PROJ-Y-130d — alle Omissionen benannt), PROJ-144 `Deployed`/**`full`** (kein offenes AC, Nachweise treffen die von der Regel gelisteten Arten). Das Wartezimmer ist damit leer; die verbleibende Altlast sind die 139 unklassifizierten Bestandszeilen → PROJ-Y-145b. Ursprüngliche Aufnahme des Konflikts zum Nachlesen: **Zähle als offen, aber nicht als unfertige Arbeit.** Beide Features sind fertig, live in Prod und getaggt (`v2.51.0-PROJ-Y-130m` bzw. `v2.52.0-PROJ-144`), stehen aber auf `Approved`, weil `features/INDEX.md` die `Deployment Scope`-Spalte noch nicht führt. Die Regel (`CLAUDE.md`, `.claude/rules/general.md`, Commit `a212171`) verlangt in diesem Fall ausdrücklich, das Bookkeeping abzubrechen statt Scopes zu erfinden. **Auflösung = eine eigene Portfolio-Migrations-Slice** (noch ohne ID; nächste freie wäre PROJ-145): Spalte einführen und die **141** als `Deployed` geführten Altzeilen evidenzbasiert klassifizieren (`full`/`mvp`/`alpha`/`tooling-only`) — kein Rückschluss aus dem alten Label, das ist echte Prüfarbeit pro Zeile. Bis dahin wächst dieses Wartezimmer mit jedem weiteren fertigen Feature; das ist der eigentliche Preis und der Grund, die Migration nicht beliebig zu verschieben. Für beide Zeilen ist der Scope aus den Nachweisen bereits belegbar (`full`). |
| PROJ-144 / AC-144.23 | Do not count as open: die einzige bewusste Verengung einer Original-Anforderung ist D-144.1 — AC-144.23 verlangt ein Modul-Gate „Backlog", aber `ModuleKey` hat keinen Backlog-Schalter, weil Backlog Kern ist. Gegatet wird über das Assistant-Modul; die Hälfte, die überhaupt existiert, ist erfüllt. Keine Folge-Slice nötig, solange Backlog kein abschaltbares Modul wird. |
| PROJ-Y-1 / PROJ-Y-2 / PROJ-Y-3 | **Mehrfach belegte Kennungen, gemessen 2026-08-20 in Tranche 3.** `PROJ-Y-1` bezeichnet vier verschiedene Followups (Register / PROJ-98 / PROJ-114 / PROJ-116), `PROJ-Y-2` ebenfalls vier, `PROJ-Y-3` drei. Ein „PROJ-Y-2 ist erledigt" aus dem Register bedeutet **nicht**, dass die gleichnamigen Followups in 98, 114 und 116 erledigt sind. Für PROJ-98 sind eindeutige IDs vergeben (98a–98d); die Umbenennung in 114 und 116 ist **PROJ-Y-145h**. Bis dahin: Followup-Kennungen dieser Familie **immer mit Elternfeature lesen**. |
